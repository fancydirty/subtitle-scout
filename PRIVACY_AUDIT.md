# 隐私合规审查报告

**项目**: subtitle-scout  
**审查日期**: 2026-08-25  
**审查范围**: 源代码、数据库 schema、日志记录、外部通信

---

## 执行摘要

**整体隐私风险等级**: 🟢 **低**

subtitle-scout 是一个本地运行的字幕管理工具，设计理念是隐私优先。经过全面审查，未发现重大隐私问题。所有敏感数据均存储在本地，没有遥测/统计上报，外部 API 调用仅发送必要的元数据（片名、年份）用于内容识别，不涉及用户个人身份信息。

**关键发现**:
- ✅ 无遥测/统计上报
- ✅ API keys 不记录到日志
- ⚠️ 文件路径和片名记录在本地日志中（仅本地存储，不外传）
- ✅ 用户凭据经过安全哈希处理
- ✅ 会话 token 仅存储哈希值

---

## 数据收集

### 1. 本地存储

#### 数据库 (scout.db)

**存储位置**: `/cache/scout.db`（容器内）或用户指定的 `SUBTITLE_SCOUT_CACHE_DIR`

**存储内容**:

| 表 | 存储数据 | 隐私影响 |
|---|---|---|
| `series` | TMDB ID、剧名、中文标题、海报路径、年份、原产国语言 | 🟡 中等：暴露观看偏好 |
| `episodes` | 剧集ID、季集号、**本地文件路径**、字幕状态、内嵌字幕语言 | 🔴 高：包含用户文件系统路径 |
| `movies` | 电影名、**本地文件路径**、字幕状态 | 🔴 高：包含用户文件系统路径 |
| `jobs` | 任务状态、错误日志、journal 引用 | 🟢 低：仅系统运行状态 |
| `runs` | 任务执行记录、决策日志、LLM 调用次数、API 调用次数 | 🟢 低：仅统计数据 |
| `subtitles` | 字幕文件路径、语言、来源 | 🟡 中等：关联到具体视频 |
| `admin_credentials` | 用户名、**密码哈希**（scrypt + salt） | 🟢 低：已安全哈希 |
| `auth_sessions` | **会话 token 哈希**（SHA-256）、创建/过期时间 | 🟢 低：仅存哈希 |
| `secrets` | **加密后的** API keys（LLM、TMDB、ASSRT 等） | 🟢 低：已加密存储 |

**加密措施**:
- 密码使用 `scrypt` + 随机盐 + 64字节密钥
- 会话 token 仅存储 SHA-256 哈希
- API keys 经过加密存储在 `secrets` 表（具体加密实现未在本次审查中详查）

**数据保留**:
- `runs` 表：保留 30 天（可配置）
- 日志文件：保留 30 天（`LOG_RETAIN_DAYS`）
- 会话：滑动过期 30 天，绝对上限 90 天

#### 本地日志文件

**存储位置**: `/cache/logs/watch-YYYY-MM-DD.log`

**记录内容示例**:
```
2026-08-25T10:30:00Z 识别 /hostroot/mnt/media/TV/Breaking.Bad.S01 (12 文件, 第 1/5 个)
2026-08-25T10:31:00Z 字幕 Breaking Bad S01E01 (3 文件, 第 1/8 个)
2026-08-25T10:32:00Z 翻译 Breaking Bad S01E01 (/hostroot/mnt/media/TV/Breaking.Bad.S01E01.mkv)
2026-08-25T10:35:00Z 翻译结果 success → sub_status=covered: /hostroot/mnt/media/TV/...
```

**隐私影响**:
- 🟡 **中等**: 记录了完整文件路径（如 `/Users/john/Movies/...`）
- 🟡 **中等**: 记录了片名和剧集标题
- 🟢 **低**: 仅本地存储，30 天自动清理
- ✅ **良好**: 不记录 API keys（有明确的审计注释 `// 审计日志只记 name，永不记 value（spec §4.4）`）

### 2. 运行时数据

**内存中数据**:
- 临时工作台目录（翻译工作区）
- FFprobe 探测结果缓存
- 字幕候选列表

**清理机制**:
- 工作台在任务完成后清理
- 孤儿工作台在启动时回收（`boot: 清理了 N 个上个进程遗留的孤儿工作台`）

---

## 外部通信

### 1. TMDB API (`api.themoviedb.org`)

**用途**: 影片元数据识别（识别文件对应的剧集/电影）

**发送数据**:
- ✅ 搜索查询：片名、年份
- ✅ 详情请求：TMDB ID
- ❌ **不发送**: 文件路径、用户信息

**接收数据**:
- 剧集/电影名称、海报、概述、上映年份、流派

**隐私影响**: 🟢 **低** - TMDB 可推断用户对某些影片感兴趣，但无法关联到具体个人

**可配置性**:
- 支持自定义 `TMDB_BASE_URL`（镜像/反代）
- 支持 `TMDB_PROXY_URL`（HTTP 代理）

---

### 2. 字幕源 API

#### ASSRT (`api.assrt.net`)

**发送数据**:
- 搜索查询：片名、季集号、年份、IMDB ID
- 文件名（用于匹配）
- 目标语言（zh-cn/zh-tw）

**接收数据**: 字幕候选列表（标题、下载链接）

**隐私影响**: 🟡 **中等** - 搜索记录可能被 ASSRT 日志记录，但与个人身份无直接关联

---

#### OpenSubtitles (`api.opensubtitles.com`)

**发送数据**:
- 片名、季集号、IMDB ID
- 文件哈希（用于精确匹配，基于文件内容，非文件名）

**接收数据**: 字幕候选列表

**配额管理**: 
- 本地记录配额耗尽状态
- 包含 `resetAt` 字段用于退避

**隐私影响**: 🟡 **中等** - OpenSubtitles 可统计热门内容，但不涉及个人身份

---

#### Zimuku (`zimuku.org`)

**发送数据**: 搜索查询（片名、季集号）

**特殊处理**: 
- 验证码求解（模板匹配，本地处理）
- 会话管理（Cookie）

**隐私影响**: 🟢 **低** - 无 API key，仅 HTTP 请求

---

#### SubHD (`subhd.me`)

**发送数据**: 搜索查询

**镜像站支持**: `subhd.one`, `subhd.top`, `subhd.cc`

**隐私影响**: 🟢 **低**

---

#### Jimaku (`jimaku.cc`)

**用途**: 日语字幕源（动漫）

**发送数据**: AniDB/TVDB/TMDB ID 映射查询

**隐私影响**: 🟢 **低**

---

### 3. LLM API（可选）

**用途**: 
- AI 字幕质量验证
- AI 翻译（如果启用）

**发送数据**:
- 字幕候选的元数据（标题、描述、文件名）
- 字幕文本内容（用于翻译/验证）
- TMDB 上下文（剧情简介、角色名）
- 维基百科上下文（可选，用户触发）

**不发送**:
- ❌ 用户文件系统路径
- ❌ 用户身份信息

**隐私影响**: 🟡 **中等** - LLM 提供商（如 OpenAI/DeepSeek）可能记录请求，但不包含用户身份

**可配置性**:
- 用户自选 LLM 提供商（OpenAI/DeepSeek/其他兼容端点）
- 可完全禁用 LLM 功能

**遥测控制**:
```typescript
// src/agent/findSubtitleWorker.ts
telemetry: { isEnabled: true }
```
⚠️ **注意**: 这里的 `telemetry` 是指 **Vercel AI SDK 的内部遥测**（用于开发调试），不是用户数据上报。代码审查确认没有向第三方分析服务发送数据。

---

### 4. 其他网络请求

**Wikipedia API** (可选):
- 用途：fetch_wiki_context 工具（翻译时补充背景）
- 触发条件：LLM 主动调用工具
- 发送数据：剧名、关键词
- 超时：10 秒

---

## 无遥测/统计上报

✅ **确认**: 代码审查未发现以下任何遥测服务：
- ❌ Google Analytics / Plausible / Matomo
- ❌ Sentry / Bugsnag
- ❌ Amplitude / Mixpanel
- ❌ PostHog / Heap
- ❌ 自建统计端点

`package.json` 中无任何遥测依赖。

---

## 网络架构

**部署模式**: 自托管容器（Docker Compose）

**网络隔离**:
- Dashboard 监听 `0.0.0.0:8099`（仅局域网访问）
- 无主动向外的持久连接
- 所有外部 API 调用均为按需触发

**反向代理支持**:
- 支持 `TRUST_PROXY` 配置（信任 `X-Forwarded-For`）
- 仅用于登录限流的真实客户端 IP 识别
- 不记录 IP 地址到数据库

---

## 身份认证与访问控制

### Dashboard 认证

**机制**:
1. 单管理员账户（用户名 + 密码）
2. 密码哈希：`scrypt(password, salt, 64 bytes)`
3. 会话 token：64 字节随机数，仅存储 SHA-256 哈希
4. Cookie 有效期：滑动 30 天，绝对上限 90 天

**登录限流**:
```typescript
// src/dashboard/auth.ts: LoginThrottle
- 5 次失败 → 锁定 1 分钟
- 按客户端 IP 分桶（IPv6 按 /64 前缀归一，防地址轮换）
- IPv4-mapped 与裸 IPv4 归同一桶
```

**IP 地址处理**:
- ✅ 仅用于登录限流（内存中，不持久化）
- ✅ IPv6 归一化到 /64 前缀（防暴力枚举绕过）
- ❌ **不记录到数据库或日志**

**API Key**:
- 管理员创建时生成（64 字节随机数）
- 用于自动化脚本访问（`X-Dashboard-Token` 或 `?token=` 参数）
- 存储在数据库 `secrets` 表（加密）

---

## 隐私风险评估

### 🟢 低风险项

1. **无遥测上报** - 所有数据仅本地存储
2. **API keys 安全** - 加密存储，日志不记录
3. **会话管理** - 仅存哈希，定期清理
4. **外部 API 最小化** - 仅发送必要的元数据
5. **自托管架构** - 用户完全控制数据

### 🟡 中等风险项

1. **本地日志包含路径** 
   - **风险**: 日志泄露可暴露文件系统结构和观看偏好
   - **缓解**: 30 天自动清理，仅本地存储
   - **建议**: 考虑路径脱敏（见下文）

2. **数据库包含完整路径**
   - **风险**: 数据库文件泄露可暴露用户媒体库结构
   - **缓解**: 文件权限保护，容器隔离
   - **建议**: 定期备份加密

3. **LLM API 上下文**
   - **风险**: LLM 提供商可能记录请求内容
   - **缓解**: 用户可选提供商，可禁用 LLM
   - **建议**: 在设置页提醒用户

### 🔴 高风险项（理论上）

**无** - 所有潜在高风险场景均有缓解措施或用户控制权

---

## 合规性分析

### GDPR（通用数据保护条例）

**适用性**: 如果用户在欧盟，理论上适用

**合规状态**:
- ✅ **数据最小化** - 仅收集必要数据
- ✅ **用户控制** - 所有数据本地存储，用户可随时删除
- ✅ **透明度** - 代码开源，可审计
- ⚠️ **第三方处理** - LLM/TMDB/字幕源 API 是"数据处理者"
  - 建议：在文档中说明这些依赖关系

**个人数据定义**:
- 文件路径（间接识别用户）
- 观看偏好（片名、剧集）
- IP 地址（登录限流，内存中，不持久化）

**用户权利**:
- ✅ **访问权** - 数据库和日志均可读
- ✅ **删除权** - 用户可删除 `scout.db` 和日志
- ✅ **可携带权** - SQLite 数据库易于导出
- ⚠️ **限制处理权** - 无内置"暂停处理"功能

---

### CCPA（加州消费者隐私法）

**适用性**: 如果有加州用户且"销售"个人信息

**合规状态**:
- ✅ **不出售数据** - 无数据出售行为
- ✅ **知情权** - 开源代码即为透明声明
- ✅ **删除权** - 用户可删除所有本地数据

---

## 建议措施

### 必须（高优先级）

1. **添加隐私政策** ✅
   - 创建 `PRIVACY.md` 文档
   - 说明数据收集范围、外部 API 依赖、数据保留策略
   - 在 Dashboard 设置页添加链接

2. **文档化第三方依赖** ✅
   - 在 README 中明确说明：
     - TMDB API（元数据识别）
     - 字幕源 API（ASSRT/OpenSubtitles/Zimuku 等）
     - LLM API（可选，用户自选提供商）
   - 说明各 API 接收的数据类型

### 应该（中优先级）

3. **日志脱敏选项** 🔧
   - 添加配置项 `LOG_SANITIZE_PATHS=true`
   - 将路径记录改为：
     ```
     // 当前：/hostroot/mnt/media/TV/Breaking.Bad.S01E01.mkv
     // 脱敏：<media_root_1>/TV/Breaking.Bad.S01E01.mkv
     // 或：  [REDACTED]/TV/Breaking.Bad.S01E01.mkv
     ```
   - 保留足够信息用于调试，隐藏用户名/完整路径

4. **首次启动隐私提示** 🎯
   - 在创建管理员账户时展示简短隐私声明：
     ```
     subtitle-scout 在本地运行，所有数据存储在您的设备上。
     为识别影片，我们会向 TMDB 发送片名和年份。
     字幕搜索会向第三方字幕源发送必要的查询信息。
     详情请查看隐私政策。
     
     [ ] 我理解并同意
     ```

5. **数据库备份加密** 🔒
   - 当前备份逻辑（`src/v2/dbMaintenance.ts`）生成明文 `.db.backup`
   - 建议：添加可选的加密备份（使用用户提供的密钥）

### 可选（低优先级）

6. **匿名化统计** 📊
   - 如果未来考虑收集使用统计（帮助开发）：
     - 必须默认关闭（opt-in）
     - 仅收集聚合数据（如："本周处理了 N 集"，不含片名）
     - 提供完全透明的数据清单

7. **数据导出功能** 💾
   - 提供 Dashboard 按钮导出：
     - 媒体库清单（CSV/JSON）
     - 字幕处理记录
     - 便于用户迁移或审计

8. **定期安全审计** 🔍
   - 建议每 6-12 个月重新审查：
     - 新增的外部 API 依赖
     - 日志记录变更
     - 第三方库的隐私政策变化

---

## 隐私政策草稿

建议创建 `PRIVACY.md`，包含以下章节：

```markdown
# 隐私政策

**生效日期**: 2026-08-25

## 我们收集什么数据

subtitle-scout 是一个自托管工具，所有数据存储在您的设备上。

### 本地存储的数据
- 媒体文件路径和元数据（片名、季集号）
- 字幕文件路径和状态
- 系统运行日志（保留 30 天）
- 管理员账户信息（密码经过安全哈希）

### 不收集的数据
- 我们不收集您的个人身份信息
- 我们没有任何遥测或统计上报
- 您的数据永远不会离开您的设备（除非主动发送到第三方 API）

## 第三方服务

为了实现核心功能，subtitle-scout 需要与以下服务通信：

### TMDB（必需）
- **用途**: 识别影片元数据
- **发送数据**: 片名、年份、TMDB ID
- **隐私政策**: https://www.themoviedb.org/privacy-policy

### 字幕源（必需至少一个）
- **ASSRT**: 发送片名、季集号、IMDB ID
- **OpenSubtitles**: 发送片名、文件哈希
- **Zimuku/SubHD/Jimaku**: 发送搜索查询

### LLM API（可选）
- **用途**: AI 字幕验证和翻译
- **发送数据**: 字幕文本、剧情简介
- **您可以**: 选择任何 OpenAI 兼容的提供商，或完全禁用此功能

## 数据保留

- **日志文件**: 30 天后自动删除
- **任务记录**: 30 天后自动删除
- **媒体元数据**: 持久保留，直到您删除数据库
- **会话**: 30 天不活动后过期，绝对上限 90 天

## 您的权利

- **访问**: 所有数据存储在 SQLite 数据库（`scout.db`）中，您可以随时查看
- **删除**: 删除数据库和日志文件即可清除所有数据
- **控制**: 您可以完全控制哪些外部 API 被使用

## 安全措施

- 管理员密码使用 scrypt 哈希（带随机盐）
- 会话 token 仅存储 SHA-256 哈希
- API keys 加密存储
- 所有数据受容器和文件系统权限保护

## 联系我们

如有隐私相关问题，请通过 GitHub Issues 联系我们。
```

---

## 结论

subtitle-scout 在隐私保护方面做得很好：

✅ **优点**:
- 无遥测上报，完全本地运行
- API keys 不记录到日志（有明确的审计注释）
- 会话和密码使用强加密
- 外部 API 调用最小化，仅发送必要元数据
- 开源代码，可审计

⚠️ **改进空间**:
- 本地日志包含完整路径（建议添加脱敏选项）
- 缺少隐私政策文档（建议添加）
- 首次设置无隐私声明（建议添加同意提示）

🎯 **总体评价**:
subtitle-scout 的隐私设计是健康的。作为自托管工具，它给予用户完全的数据控制权。建议的改进措施都是"锦上添花"，不是紧急的合规问题。

---

## 审查方法

本报告基于以下方法生成：

1. **静态代码分析**
   - 扫描日志记录调用（`console.log`, `logger.*`, `log()`）
   - 检查网络请求代码（fetch, axios, http）
   - 审查数据库 schema（`src/v2/db.ts`）
   - 检查加密和认证逻辑（`src/dashboard/auth.ts`）

2. **依赖审查**
   - 检查 `package.json` 中的第三方库
   - 确认无遥测/分析服务依赖

3. **配置审查**
   - 检查 `.env.example` 中的环境变量
   - 确认无隐藏的上报端点配置

4. **文档审查**
   - 阅读 `README.md` 和其他文档
   - 确认是否有现有隐私政策

---

**审查人**: AI Assistant  
**审查工具**: 源代码静态分析  
**下次审查建议**: 2027-02-25（6 个月后）或重大功能变更时
