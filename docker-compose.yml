# v(next)：Jellyfin 不再是运行依赖——subtitle-scout 直接扫描磁盘 + TMDB 识别文件，
# 不再打任何媒体服务器的 API。若你之前跑过旧版本，可随时移除 jellyfin 容器/相关 env
# （JELLYFIN_URL / JELLYFIN_API_KEY / MEDIA_PATH_MAPPINGS）——不影响 scout 继续工作。
# 想同时用 Jellyfin 当播放器（和 scout 的字幕功能完全无关，纯粹图省事）？见 docker-compose.bundle.yml。
services:
  subtitle-scout:
    image: ghcr.io/fancydirty/subtitle-scout:latest
    container_name: subtitle-scout
    restart: unless-stopped
    # 容器内 PID 1 默认是 node（Dockerfile CMD 直接跑 dist/cli/index.js）——普通进程不具备
    # init 的孤儿收割职责,异常退出时子孙进程会被重新收养给 PID 1 而 node 不 wait() 它们。
    # init:true 让 Docker 注入内置 tini 作真正的 PID 1,负责收割 + 正确转发终止信号。
    # （历史背景:旧管线曾 detached 起 subtitle-fetch 子进程,僵尸问题更突出;v3 worker 全部
    # 进程内调 adapter,但 tini 的信号/收割卫生依然正确。）
    init: true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "${DASHBOARD_PORT:-8099}:${DASHBOARD_PORT:-8099}"
    # 让 Linux 上也能用 http://host.docker.internal 访问宿主机（macOS/Windows 默认可用）
    # ——例如 LLM_BASE_URL 指向宿主机上跑的本地模型端点（Ollama/LM Studio 等）。
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      LLM_BASE_URL: ${LLM_BASE_URL}
      LLM_API_KEY: ${LLM_API_KEY}
      LLM_MODEL: ${LLM_MODEL}
      LLM_EXTRA_BODY: ${LLM_EXTRA_BODY:-}
      ASSRT_TOKEN: ${ASSRT_TOKEN}
      TMDB_API_KEY: ${TMDB_API_KEY}
      OPENSUBTITLES_API_KEY: ${OPENSUBTITLES_API_KEY:-}
      OPENSUBTITLES_USERNAME: ${OPENSUBTITLES_USERNAME:-}
      OPENSUBTITLES_PASSWORD: ${OPENSUBTITLES_PASSWORD:-}
      # 字幕源开关：默认关（各自需额外配置）。docker compose 只注入本块显式列出的变量，
      # 缺列时 .env 里的 SUBHD_ENABLED/ZIMUKU_ENABLED=true 到不了容器 → 源静默不生效。
      ZIMUKU_ENABLED: ${ZIMUKU_ENABLED:-false}
      SUBHD_ENABLED: ${SUBHD_ENABLED:-false}
      # TMDB 反代/代理（墙内直连常被墙时用;不配则默认直连官方 API）
      TMDB_BASE_URL: ${TMDB_BASE_URL:-}
      TMDB_PROXY_URL: ${TMDB_PROXY_URL:-}
      # jimaku 日字源(F2);不填则该 adapter 休眠
      JIMAKU_API_KEY: ${JIMAKU_API_KEY:-}
      # AI 翻译部署门:三件套缺一,daemon 自动翻译整体休眠(手动 translate-item 不受影响)
      TRANSLATE_BASE_URL: ${TRANSLATE_BASE_URL:-}
      TRANSLATE_API_KEY: ${TRANSLATE_API_KEY:-}
      TRANSLATE_MODEL: ${TRANSLATE_MODEL:-}
      TRANSLATE_CRITIC: ${TRANSLATE_CRITIC:-}
      TRANSLATE_CRITIC_MODEL: ${TRANSLATE_CRITIC_MODEL:-}
      TRANSLATE_TIMEOUT_MS: ${TRANSLATE_TIMEOUT_MS:-}
      # 目标字幕语言与扫描节奏(dashboard 设置页的行为级配置优先于这两个种子值)
      TARGET_LANGUAGES: ${TARGET_LANGUAGES:-}
      SCAN_INTERVAL_MS: ${SCAN_INTERVAL_MS:-}
      LOG_RETAIN_DAYS: ${LOG_RETAIN_DAYS:-}
      # 媒体守备目录(dashboard 设置页优先;此处为首启种子,容器内路径)
      MEDIA_ROOTS: ${MEDIA_ROOTS:-/media}
      SUBTITLE_SCOUT_CACHE_DIR: /cache
      TZ: ${TZ:-Asia/Shanghai}
      DASHBOARD_PORT: ${DASHBOARD_PORT:-8099}
      DASHBOARD_TOKEN: ${DASHBOARD_TOKEN:-}
    volumes:
      # 宿主机 Movies/ TV/ → 容器内 movies/ tv/(小写,Linux 容器大小写敏感)
      # MEDIA_ROOTS 应写 /media/movies,/media/tv(对应容器内路径)
      - ${MEDIA_HOST_PATH}/Movies:/media/movies
      - ${MEDIA_HOST_PATH}/TV:/media/tv
      - ./cache:/cache
