# 鉴权 A1-A4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DASHBOARD_TOKEN 裸奔时代结束——Sonarr 式单管理员（用户名+密码，首启向导），session cookie + API key 双通道，旧 DASHBOARD_TOKEN 兼容；顺带关闭 R2D-2（fs/list 未鉴权全盘枚举）。

**Architecture:** 新模块 `src/dashboard/auth.ts` 承载全部纯逻辑（scrypt 哈希/会话仓/限流/AuthService），`server.ts` 加统一前置门（所有 `/api/*` 先过 auth，静态资源永放行），撤掉现存 8 处散布的 per-branch token 门与 `handleApiRoute` 的 token 参数（前置门是唯一门）。前端加 `/setup`、`/login` 两页 + app-shell 鉴权重定向 + Settings「Security」区。凭据存 settings 表三键（schema 零迁移）。

**Tech Stack:** node:crypto（scrypt/randomBytes/timingSafeEqual，零新依赖）、内存 Map 会话（重启即全员重登，spec 定案 YAGNI）、React SPA（现有 web/ 栈）、vitest。

**Spec:** `docs/design/2026-07-17-auth-design.md`。四阶段：A1 服务端凭据+门（Task 1-7）→ A2 前端向导+登录（Task 8-11）→ A3 Settings Security 区（Task 12-14）→ A4 兼容与验收（Task 15-17）。

**裁决记录（偏离决策台账一处，如实声明）：** 台账里 fs/list 的双保险第二道原写"显式约束 path 必须在媒体根内"——实勘 `listMediaSubdirs` 就是**加根目录选择器本体**（用户从 / 往下浏览挑选新根，Jellyfin/Sonarr 同款），约束在既有根内会自毁其用途（第一个根都加不了）。改为：第二道=未初始化态一律 401（向导阶段无匿名浏览；R2D-2 的攻击面=未鉴权枚举，前置门+未初始化拒绝已完整关闭），并保留既有"只列目录名、绝不列文件"边界。

---

## File Structure

```
src/dashboard/auth.ts            # 新：hashPassword/verifyPassword、SessionStore、LoginThrottle、AuthService
src/dashboard/auth.test.ts       # 新：上述纯逻辑单测
src/dashboard/server.ts          # 改：统一前置门 + 4 个 auth 端点 + 撤 per-branch token 门
src/dashboard/server.test.ts     # 改：token 旧测试改写为新世界（legacy 兼容测试保留语义）
src/dashboard/router.ts          # 改：handleApiRoute 撤 token 参数（门上移到 server.ts 前置）
src/dashboard/router.test.ts     # 改：撤 token 相关断言
src/cli/index.ts                 # 改（A4）：启动播报改口
src/cli/dashboardTokenWarning.ts # 改（A4）：告警三态改写
web/src/...                      # A2/A3（细节见各 Task）
README.md                        # 改（A4）：鉴权说明 + 反代 TLS 建议
```

---

## A1 服务端凭据与门

### Task 1: 密码哈希（scrypt，`盐:哈希`）

**Files:**
- Create: `src/dashboard/auth.ts`
- Test: `src/dashboard/auth.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
// src/dashboard/auth.test.ts
import { describe, it, expect } from 'vitest'
import { hashPassword, verifyPassword } from './auth.js'

describe('hashPassword/verifyPassword（scrypt，盐:哈希 hex 格式）', () => {
  it('同一密码两次哈希产出不同串（随机盐），但都能通过校验', () => {
    const h1 = hashPassword('correct horse')
    const h2 = hashPassword('correct horse')
    expect(h1).not.toBe(h2)
    expect(h1).toMatch(/^[0-9a-f]{32}:[0-9a-f]{128}$/) // 16B 盐 + 64B 哈希
    expect(verifyPassword('correct horse', h1)).toBe(true)
    expect(verifyPassword('correct horse', h2)).toBe(true)
  })
  it('错误密码校验失败', () => {
    expect(verifyPassword('wrong', hashPassword('right'))).toBe(false)
  })
  it('存储串畸形（缺冒号/非 hex）→ false 而非抛错', () => {
    expect(verifyPassword('x', 'not-a-hash')).toBe(false)
    expect(verifyPassword('x', '')).toBe(false)
    expect(verifyPassword('x', 'zz:zz')).toBe(false)
  })
})
```

- [ ] **Step 2: Run to verify RED**

Run: `cd /Users/dirtyfancy/projects/subtitle-scout && npx vitest run src/dashboard/auth.test.ts`
Expected: FAIL — `Cannot find module './auth.js'`（模块尚不存在，符合预期）

- [ ] **Step 3: Minimal implementation**

```ts
// src/dashboard/auth.ts
import { scryptSync, randomBytes, timingSafeEqual } from 'node:crypto'

/** 鉴权战役 A1（spec 2026-07-17）：单管理员凭据的纯逻辑层——哈希/会话/限流/服务编排全在
 *  这里，server.ts 只做 HTTP 接线。零新依赖（node:crypto scrypt）。 */

const SALT_BYTES = 16
const KEY_BYTES = 64

export function hashPassword(password: string): string {
  const salt = randomBytes(SALT_BYTES)
  const hash = scryptSync(password, salt, KEY_BYTES)
  return `${salt.toString('hex')}:${hash.toString('hex')}`
}

/** 畸形存储串（缺冒号/非 hex/长度不符）一律 false——绝不抛错炸到 server.ts。 */
export function verifyPassword(password: string, stored: string): boolean {
  const idx = stored.indexOf(':')
  if (idx <= 0) return false
  const saltHex = stored.slice(0, idx)
  const hashHex = stored.slice(idx + 1)
  if (!/^[0-9a-f]+$/.test(saltHex) || !/^[0-9a-f]+$/.test(hashHex)) return false
  const expected = Buffer.from(hashHex, 'hex')
  const actual = scryptSync(password, Buffer.from(saltHex, 'hex'), expected.length || KEY_BYTES)
  return actual.length === expected.length && timingSafeEqual(actual, expected)
}
```

- [ ] **Step 4: Run to verify GREEN**

Run: `npx vitest run src/dashboard/auth.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add src/dashboard/auth.ts src/dashboard/auth.test.ts
git commit -m "feat(auth): scrypt 密码哈希（盐:哈希，node:crypto 零新依赖）"
```

### Task 2: SessionStore（内存 Map + 30 天滚动过期）

**Files:**
- Modify: `src/dashboard/auth.ts`
- Test: `src/dashboard/auth.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
// 追加到 src/dashboard/auth.test.ts
import { SessionStore } from './auth.js'   // 并入顶部 import 行

describe('SessionStore（内存 Map，30 天滚动过期）', () => {
  const NOW = 1_700_000_000_000
  const DAY = 24 * 3600 * 1000
  it('create 签发 64 hex token，verify 通过', () => {
    const s = new SessionStore()
    const t = s.create(NOW)
    expect(t).toMatch(/^[0-9a-f]{64}$/)
    expect(s.verify(t, NOW + 1000)).toBe(true)
  })
  it('过期后 verify false 且条目被清', () => {
    const s = new SessionStore()
    const t = s.create(NOW)
    expect(s.verify(t, NOW + 31 * DAY)).toBe(false)
    expect(s.verify(t, NOW + 1000)).toBe(false) // 已删，回到过期前的时刻也不行
  })
  it('滚动过期：每次 verify 续期 30 天', () => {
    const s = new SessionStore()
    const t = s.create(NOW)
    expect(s.verify(t, NOW + 29 * DAY)).toBe(true)  // 续期到 +59d
    expect(s.verify(t, NOW + 58 * DAY)).toBe(true)  // 仍活着
  })
  it('revoke 后 verify false；未知 token false', () => {
    const s = new SessionStore()
    const t = s.create(NOW)
    s.revoke(t)
    expect(s.verify(t, NOW)).toBe(false)
    expect(s.verify('deadbeef', NOW)).toBe(false)
  })
})
```

- [ ] **Step 2: Run to verify RED** — `npx vitest run src/dashboard/auth.test.ts`，新 4 条 FAIL（SessionStore 未导出）

- [ ] **Step 3: Minimal implementation**

```ts
// 追加到 src/dashboard/auth.ts
const SESSION_TTL_MS = 30 * 24 * 3600 * 1000

/** session cookie 后端仓：内存 Map（spec 定案——重启全员重登，YAGNI 不落盘）。滚动过期：
 *  verify 通过即续期 30 天。 */
export class SessionStore {
  private sessions = new Map<string, number>() // token → expiresAt

  create(now: number): string {
    const token = randomBytes(32).toString('hex')
    this.sessions.set(token, now + SESSION_TTL_MS)
    return token
  }

  verify(token: string, now: number): boolean {
    const exp = this.sessions.get(token)
    if (exp === undefined) return false
    if (now > exp) {
      this.sessions.delete(token)
      return false
    }
    this.sessions.set(token, now + SESSION_TTL_MS)
    return true
  }

  revoke(token: string): void {
    this.sessions.delete(token)
  }
}
```

- [ ] **Step 4: Run to verify GREEN** — 全文件 PASS
- [ ] **Step 5: Commit** — `git commit -m "feat(auth): 内存会话仓（30 天滚动过期）"`

### Task 3: LoginThrottle（5 次/分钟，内存计数）

**Files:**
- Modify: `src/dashboard/auth.ts`
- Test: `src/dashboard/auth.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
// 追加到 src/dashboard/auth.test.ts（import 行并入 LoginThrottle）
describe('LoginThrottle（5 次/分钟/来源，内存计数）', () => {
  const NOW = 1_700_000_000_000
  it('同一来源 60s 窗口内前 5 次放行，第 6 次拒绝', () => {
    const th = new LoginThrottle()
    for (let i = 0; i < 5; i++) expect(th.allow('1.2.3.4', NOW + i * 1000)).toBe(true)
    expect(th.allow('1.2.3.4', NOW + 5000)).toBe(false)
  })
  it('窗口滚过（61s 后）计数重置', () => {
    const th = new LoginThrottle()
    for (let i = 0; i < 6; i++) th.allow('1.2.3.4', NOW)
    expect(th.allow('1.2.3.4', NOW + 61_000)).toBe(true)
  })
  it('不同来源互不影响', () => {
    const th = new LoginThrottle()
    for (let i = 0; i < 6; i++) th.allow('1.2.3.4', NOW)
    expect(th.allow('5.6.7.8', NOW)).toBe(true)
  })
})
```

- [ ] **Step 2: Run to verify RED** — 新 3 条 FAIL（LoginThrottle 未导出）

- [ ] **Step 3: Minimal implementation**

```ts
// 追加到 src/dashboard/auth.ts
/** 登录失败节流（spec §2：内存计数 5 次/分钟）。按"尝试"计数而非"失败"——比 spec 略严但
 *  实现更简单且更保守（合法用户 1 次就登进去了，感受不到差别）。 */
export class LoginThrottle {
  private counts = new Map<string, { windowStart: number; count: number }>()
  constructor(private limit = 5, private windowMs = 60_000) {}

  allow(key: string, now: number): boolean {
    const entry = this.counts.get(key)
    if (!entry || now - entry.windowStart >= this.windowMs) {
      this.counts.set(key, { windowStart: now, count: 1 })
      return true
    }
    entry.count++
    return entry.count <= this.limit
  }
}
```

- [ ] **Step 4: Run to verify GREEN** — 全文件 PASS
- [ ] **Step 5: Commit** — `git commit -m "feat(auth): 登录节流（5 次/分钟/来源）"`

### Task 4: AuthService（settings 三键编排：setup/login/api key/改密）

**Files:**
- Modify: `src/dashboard/auth.ts`
- Test: `src/dashboard/auth.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
// 追加到 src/dashboard/auth.test.ts。真 in-mem db（项目铁律：repo 层不用 mock）：
import { openDb } from '../v2/db.js'
import { SettingsRepo } from '../v2/settingsRepo.js'
import { AuthService } from './auth.js'

describe('AuthService（settings 三键：auth_username/auth_password_hash/auth_api_key）', () => {
  const NOW = 1_700_000_000_000
  function mkAuth() {
    return new AuthService(new SettingsRepo(openDb(':memory:')))
  }
  it('未初始化态：isInitialized false；setup 写三键并返回 32 hex api key', () => {
    const auth = mkAuth()
    expect(auth.isInitialized()).toBe(false)
    const r = auth.setup('admin', 'hunter2222', NOW)
    expect(r.ok).toBe(true)
    if (r.ok) expect(r.apiKey).toMatch(/^[0-9a-f]{32}$/)
    expect(auth.isInitialized()).toBe(true)
  })
  it('setup 一次性：已初始化后再 setup → ok:false', () => {
    const auth = mkAuth()
    auth.setup('admin', 'hunter2222', NOW)
    expect(auth.setup('evil', 'x'.repeat(10), NOW).ok).toBe(false)
  })
  it('setup 校验：用户名空/密码短于 8 → ok:false 且不写键', () => {
    const auth = mkAuth()
    expect(auth.setup('', 'longenough', NOW).ok).toBe(false)
    expect(auth.setup('admin', 'short', NOW).ok).toBe(false)
    expect(auth.isInitialized()).toBe(false)
  })
  it('login：对的用户名+密码 → sessionToken；错的 → status 401；节流爆表 → status 429', () => {
    const auth = mkAuth()
    auth.setup('admin', 'hunter2222', NOW)
    const ok = auth.login('admin', 'hunter2222', '1.1.1.1', NOW)
    expect(ok.ok).toBe(true)
    if (ok.ok) expect(auth.sessions.verify(ok.sessionToken, NOW + 1)).toBe(true)
    const bad = auth.login('admin', 'wrong', '1.1.1.1', NOW)
    expect(bad).toMatchObject({ ok: false, status: 401 })
    for (let i = 0; i < 5; i++) auth.login('admin', 'wrong', '2.2.2.2', NOW)
    expect(auth.login('admin', 'hunter2222', '2.2.2.2', NOW)).toMatchObject({ ok: false, status: 429 })
  })
  it('verifyApiKey：setup 发的 key 通过；错 key/未初始化 → false', () => {
    const auth = mkAuth()
    expect(auth.verifyApiKey('anything')).toBe(false)
    const r = auth.setup('admin', 'hunter2222', NOW)
    if (!r.ok) throw new Error('setup failed')
    expect(auth.verifyApiKey(r.apiKey)).toBe(true)
    expect(auth.verifyApiKey('0'.repeat(32))).toBe(false)
  })
  it('changePassword：旧密码对才改；改后旧密码登不进、新密码能登', () => {
    const auth = mkAuth()
    auth.setup('admin', 'hunter2222', NOW)
    expect(auth.changePassword('wrong-old', 'newpass888', NOW).ok).toBe(false)
    expect(auth.changePassword('hunter2222', 'newpass888', NOW).ok).toBe(true)
    expect(auth.login('admin', 'hunter2222', '3.3.3.3', NOW).ok).toBe(false)
    expect(auth.login('admin', 'newpass888', '3.3.3.3', NOW).ok).toBe(true)
  })
  it('regenerateApiKey：换新 key，旧 key 失效；apiKeyTail 给尾 4 位', () => {
    const auth = mkAuth()
    const r = auth.setup('admin', 'hunter2222', NOW)
    if (!r.ok) throw new Error('setup failed')
    const nk = auth.regenerateApiKey(NOW)
    expect(nk).toMatch(/^[0-9a-f]{32}$/)
    expect(auth.verifyApiKey(r.apiKey)).toBe(false)
    expect(auth.verifyApiKey(nk)).toBe(true)
    expect(auth.apiKeyTail()).toBe(nk.slice(-4))
  })
})
```

- [ ] **Step 2: Run to verify RED** — 新 7 条 FAIL（AuthService 未导出）

- [ ] **Step 3: Minimal implementation**

```ts
// 追加到 src/dashboard/auth.ts
import type { SettingsRepo } from '../v2/settingsRepo.js'   // 并入顶部 import

export const AUTH_KEYS = {
  username: 'auth_username',
  passwordHash: 'auth_password_hash',
  apiKey: 'auth_api_key',
} as const

const MIN_PASSWORD_LEN = 8

export type SetupResult = { ok: true; apiKey: string } | { ok: false; error: string }
export type LoginResult = { ok: true; sessionToken: string } | { ok: false; status: 401 | 429; error: string }
export type ChangePasswordResult = { ok: true } | { ok: false; error: string }

/** settings 三键之上的鉴权编排。会话/节流自带默认实例（server.ts 一个 AuthService 就是一套
 *  完整状态），测试可直接摸 .sessions 断言。 */
export class AuthService {
  readonly sessions = new SessionStore()
  private throttle = new LoginThrottle()

  constructor(private settings: SettingsRepo) {}

  isInitialized(): boolean {
    return this.settings.get(AUTH_KEYS.passwordHash) !== null
  }

  setup(username: string, password: string, now: number): SetupResult {
    if (this.isInitialized()) return { ok: false, error: 'already initialized' }
    if (!username.trim()) return { ok: false, error: 'username is required' }
    if (password.length < MIN_PASSWORD_LEN) return { ok: false, error: `password must be at least ${MIN_PASSWORD_LEN} characters` }
    const apiKey = randomBytes(16).toString('hex')
    this.settings.set(AUTH_KEYS.username, username.trim(), now)
    this.settings.set(AUTH_KEYS.passwordHash, hashPassword(password), now)
    this.settings.set(AUTH_KEYS.apiKey, apiKey, now)
    return { ok: true, apiKey }
  }

  login(username: string, password: string, remoteAddr: string, now: number): LoginResult {
    if (!this.throttle.allow(remoteAddr, now)) return { ok: false, status: 429, error: 'too many attempts — wait a minute' }
    const storedUser = this.settings.get(AUTH_KEYS.username)
    const storedHash = this.settings.get(AUTH_KEYS.passwordHash)
    if (storedUser === null || storedHash === null) return { ok: false, status: 401, error: 'not initialized' }
    if (username !== storedUser || !verifyPassword(password, storedHash)) {
      return { ok: false, status: 401, error: 'invalid username or password' }
    }
    return { ok: true, sessionToken: this.sessions.create(now) }
  }

  /** 常量时间比较（同长才比，node timingSafeEqual 要求等长）。 */
  verifyApiKey(key: string): boolean {
    const stored = this.settings.get(AUTH_KEYS.apiKey)
    if (stored === null || key.length !== stored.length) return false
    return timingSafeEqual(Buffer.from(key), Buffer.from(stored))
  }

  changePassword(oldPassword: string, newPassword: string, now: number): ChangePasswordResult {
    const storedHash = this.settings.get(AUTH_KEYS.passwordHash)
    if (storedHash === null) return { ok: false, error: 'not initialized' }
    if (!verifyPassword(oldPassword, storedHash)) return { ok: false, error: 'current password is incorrect' }
    if (newPassword.length < MIN_PASSWORD_LEN) return { ok: false, error: `password must be at least ${MIN_PASSWORD_LEN} characters` }
    this.settings.set(AUTH_KEYS.passwordHash, hashPassword(newPassword), now)
    return { ok: true }
  }

  regenerateApiKey(now: number): string {
    const apiKey = randomBytes(16).toString('hex')
    this.settings.set(AUTH_KEYS.apiKey, apiKey, now)
    return apiKey
  }

  apiKeyTail(): string | null {
    const stored = this.settings.get(AUTH_KEYS.apiKey)
    return stored === null ? null : stored.slice(-4)
  }
}
```

- [ ] **Step 4: Run to verify GREEN** — `npx vitest run src/dashboard/auth.test.ts` 全 PASS
- [ ] **Step 5: Commit** — `git commit -m "feat(auth): AuthService——setup/login/api key/改密编排（settings 三键）"`

### Task 5: server.ts 统一前置门 + 4 个 auth 端点

**Files:**
- Modify: `src/dashboard/server.ts`
- Test: `src/dashboard/server.test.ts`

前置门语义（spec §2，全部集中在 createServer 回调顶部，静态资源不设防）：

| 态 | 请求 | 结果 |
|---|---|---|
| 未初始化 | `POST /api/v2/auth/setup` | 放行（写三键，一次性） |
| 未初始化 | `GET /api/v2/auth/status` | 放行（前端探测用） |
| 未初始化 | 带有效 legacy DASHBOARD_TOKEN 的任意 `/api/*` | 放行（兼容：旧部署不破坏） |
| 未初始化 | 其余 `/api/*` | 401 `{error:'setup required'}` |
| 已初始化 | `POST /api/v2/auth/login` / `GET /api/v2/auth/status` | 放行 |
| 已初始化 | session cookie（`scout_session`）有效 | 放行 |
| 已初始化 | `X-Api-Key` 头 或 `?apikey=` query 命中 | 放行（SSE/脚本通道） |
| 已初始化 | legacy `?token=`/`x-dashboard-token` 命中配置的 DASHBOARD_TOKEN | 放行（等价 api key） |
| 已初始化 | `POST /api/v2/auth/setup` | 403 `{error:'already initialized'}` |
| 已初始化 | 其余 | 401 `{error:'unauthorized'}` |

- [ ] **Step 1: Write the failing tests**（追加到 `src/dashboard/server.test.ts`；沿用文件里现成的 `start()`/`distWith()` 基座）

```ts
describe('auth 前置门（A1：统一门，spec §2）', () => {
  it('未初始化：/api/v2/library 401 setup required；静态资源照常 200', async () => {
    const { base } = await start(distWith('<!doctype html><title>scout</title>'))
    const api = await fetch(`${base}/api/v2/library`)
    expect(api.status).toBe(401)
    expect(await api.json()).toEqual({ error: 'setup required' })
    const page = await fetch(`${base}/`)
    expect(page.status).toBe(200)
  })
  it('setup：POST 写三键返回 apiKey + 签发 session cookie；二次 setup 403', async () => {
    const { base } = await start(distWith('x'))
    const r = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    expect(r.status).toBe(200)
    const body = await r.json() as { apiKey: string }
    expect(body.apiKey).toMatch(/^[0-9a-f]{32}$/)
    expect(r.headers.get('set-cookie')).toMatch(/^scout_session=[0-9a-f]{64}; /)
    const again = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'x', password: 'y'.repeat(10) }),
    })
    expect(again.status).toBe(403)
  })
  it('login/logout：对密码拿 cookie 后 API 通；logout 后同 cookie 401', async () => {
    const { base } = await start(distWith('x'))
    await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const login = await fetch(`${base}/api/v2/auth/login`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    expect(login.status).toBe(200)
    const cookie = (login.headers.get('set-cookie') ?? '').split(';')[0]
    const lib = await fetch(`${base}/api/v2/library`, { headers: { cookie } })
    expect(lib.status).toBe(200)
    const out = await fetch(`${base}/api/v2/auth/logout`, { method: 'POST', headers: { cookie } })
    expect(out.status).toBe(200)
    const after = await fetch(`${base}/api/v2/library`, { headers: { cookie } })
    expect(after.status).toBe(401)
  })
  it('login 错密码 401；连爆 6 次 429（节流）', async () => {
    const { base } = await start(distWith('x'))
    await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const bad = () => fetch(`${base}/api/v2/auth/login`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'nope-nope' }),
    })
    expect((await bad()).status).toBe(401)
    for (let i = 0; i < 4; i++) await bad()
    expect((await bad()).status).toBe(429)
  })
  it('X-Api-Key 头与 ?apikey= query 都走通（SSE/脚本通道）', async () => {
    const { base } = await start(distWith('x'))
    const setup = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const { apiKey } = await setup.json() as { apiKey: string }
    expect((await fetch(`${base}/api/v2/library`, { headers: { 'x-api-key': apiKey } })).status).toBe(200)
    expect((await fetch(`${base}/api/v2/library?apikey=${apiKey}`)).status).toBe(200)
    expect((await fetch(`${base}/api/v2/library`, { headers: { 'x-api-key': 'wrong' } })).status).toBe(401)
  })
  it('auth/status：未初始化 {initialized:false,...}；登录后 authenticated:true', async () => {
    const { base } = await start(distWith('x'))
    const s1 = await fetch(`${base}/api/v2/auth/status`)
    expect(await s1.json()).toEqual({ initialized: false, authenticated: false })
    const setup = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const cookie = (setup.headers.get('set-cookie') ?? '').split(';')[0]
    const s2 = await fetch(`${base}/api/v2/auth/status`, { headers: { cookie } })
    expect(await s2.json()).toEqual({ initialized: true, authenticated: true })
    const s3 = await fetch(`${base}/api/v2/auth/status`)
    expect(await s3.json()).toEqual({ initialized: true, authenticated: false })
  })
  it('legacy DASHBOARD_TOKEN：未初始化+带旧 token → API 照常通（旧部署零破坏）', async () => {
    const { base } = await start(distWith('x'), 'legacy-tok')
    expect((await fetch(`${base}/api/v2/library?token=legacy-tok`)).status).toBe(200)
    expect((await fetch(`${base}/api/v2/library`, { headers: { 'x-dashboard-token': 'legacy-tok' } })).status).toBe(200)
    expect((await fetch(`${base}/api/v2/library?token=wrong`)).status).toBe(401)
  })
  it('已初始化后 legacy token 仍等价 api key（迁移期共存）', async () => {
    const { base } = await start(distWith('x'), 'legacy-tok')
    await fetch(`${base}/api/v2/auth/setup?token=legacy-tok`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    expect((await fetch(`${base}/api/v2/library?token=legacy-tok`)).status).toBe(200)
  })
  it('SSE trace-stream 走 ?apikey=（EventSource 无法带头）', async () => {
    const { base } = await start(distWith('x'))
    const setup = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const { apiKey } = await setup.json() as { apiKey: string }
    const ac = new AbortController()
    const sse = await fetch(`${base}/api/v2/workflow/trace-stream?apikey=${apiKey}`, { signal: ac.signal })
    expect(sse.status).toBe(200)
    expect(sse.headers.get('content-type')).toContain('text/event-stream')
    ac.abort()
    expect((await fetch(`${base}/api/v2/workflow/trace-stream`)).status).toBe(401)
  })
})
```

- [ ] **Step 2: Run to verify RED** — `npx vitest run src/dashboard/server.test.ts -t 'auth 前置门'`，9 条全 FAIL（端点不存在/门不存在）。同时预期**既有**测试大量变红（无鉴权直接打 API 的旧世界断言）——先别动它们，Step 3 实现后统一看余波，Task 6 处理。

- [ ] **Step 3: Implementation**（server.ts 顶部改造）

```ts
// server.ts 新增 import
import { AuthService } from './auth.js'

// startDashboard 内、deps 定义之前：
const auth = new AuthService(settingsRepo)

// createServer 回调内，const rawPath = url.pathname 之后、一切既有分支之前，插入统一前置门：

      // ---- 鉴权 A1：统一前置门（spec §2）。静态资源不设防（SPA 壳/login 页本身要能加载），
      // /api/* 全部先过这里。cookie/api key/legacy token 三通道；未初始化只放 setup/status。----
      if (rawPath.startsWith('/api/')) {
        const cookies = parseCookies(req.headers.cookie)
        const sessionToken = cookies['scout_session']
        const apiKeyReq = (req.headers['x-api-key'] as string | undefined) ?? url.searchParams.get('apikey') ?? undefined
        const legacyReq = url.searchParams.get('token') ?? (req.headers['x-dashboard-token'] as string | undefined)
        const authed =
          (sessionToken !== undefined && auth.sessions.verify(sessionToken, Date.now())) ||
          (apiKeyReq !== undefined && auth.verifyApiKey(apiKeyReq)) ||
          (token !== undefined && legacyReq === token)

        // 探测端点：任何态放行（前端 app-shell 靠它决定去 /setup、/login 还是正常渲染）
        if (rawPath === '/api/v2/auth/status' && req.method === 'GET') {
          res.writeHead(200, { 'content-type': 'application/json; charset=utf-8' })
          res.end(JSON.stringify({ initialized: auth.isInitialized(), authenticated: authed }))
          return
        }

        if (rawPath === '/api/v2/auth/setup') {
          if (req.method !== 'POST') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
          if (auth.isInitialized()) { res.writeHead(403, JSON_CT); res.end(JSON.stringify({ error: 'already initialized' })); return }
          const body = await readJsonBody(req)
          if (body === undefined) { res.writeHead(400, JSON_CT); res.end(JSON.stringify({ error: 'invalid JSON body' })); return }
          const b = (body ?? {}) as { username?: unknown; password?: unknown }
          const r = auth.setup(
            typeof b.username === 'string' ? b.username : '',
            typeof b.password === 'string' ? b.password : '', Date.now(),
          )
          if (!r.ok) { res.writeHead(400, JSON_CT); res.end(JSON.stringify({ error: r.error })); return }
          // 成功即登录（spec §3 向导语义）：直接签发 session cookie
          const t = auth.sessions.create(Date.now())
          res.writeHead(200, { ...JSON_CT, 'set-cookie': sessionCookie(t) })
          res.end(JSON.stringify({ ok: true, apiKey: r.apiKey }))
          return
        }

        if (rawPath === '/api/v2/auth/login') {
          if (req.method !== 'POST') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
          const body = await readJsonBody(req)
          if (body === undefined) { res.writeHead(400, JSON_CT); res.end(JSON.stringify({ error: 'invalid JSON body' })); return }
          const b = (body ?? {}) as { username?: unknown; password?: unknown }
          const r = auth.login(
            typeof b.username === 'string' ? b.username : '',
            typeof b.password === 'string' ? b.password : '',
            req.socket.remoteAddress ?? 'unknown', Date.now(),
          )
          if (!r.ok) { res.writeHead(r.status, JSON_CT); res.end(JSON.stringify({ error: r.error })); return }
          res.writeHead(200, { ...JSON_CT, 'set-cookie': sessionCookie(r.sessionToken) })
          res.end(JSON.stringify({ ok: true }))
          return
        }

        if (rawPath === '/api/v2/auth/logout') {
          if (req.method !== 'POST') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
          if (sessionToken) auth.sessions.revoke(sessionToken)
          res.writeHead(200, { ...JSON_CT, 'set-cookie': 'scout_session=; HttpOnly; Path=/; Max-Age=0; SameSite=Lax' })
          res.end(JSON.stringify({ ok: true }))
          return
        }

        if (!authed) {
          res.writeHead(401, JSON_CT)
          res.end(JSON.stringify({ error: auth.isInitialized() ? 'unauthorized' : 'setup required' }))
          return
        }
      }
```

模块级辅助（server.ts 底部或 CONTENT_TYPES 附近）：

```ts
const JSON_CT = { 'content-type': 'application/json; charset=utf-8' }

function parseCookies(header: string | undefined): Record<string, string> {
  const out: Record<string, string> = {}
  if (!header) return out
  for (const part of header.split(';')) {
    const idx = part.indexOf('=')
    if (idx > 0) out[part.slice(0, idx).trim()] = part.slice(idx + 1).trim()
  }
  return out
}

function sessionCookie(token: string): string {
  // 无 HTTPS（家庭局域网形态，spec §2 安全边界如实）故无 Secure；反代加 TLS 见 README
  return `scout_session=${token}; HttpOnly; Path=/; Max-Age=2592000; SameSite=Lax`
}

/** JSON body 读取：解析失败返回 undefined（调用方 400），空 body 视作 {}。 */
async function readJsonBody(req: import('node:http').IncomingMessage): Promise<unknown> {
  let raw = ''
  for await (const chunk of req) raw += chunk
  try {
    return JSON.parse(raw || '{}')
  } catch {
    return undefined
  }
}
```

- [ ] **Step 4: Run to verify** — `npx vitest run src/dashboard/server.test.ts -t 'auth 前置门'` 9 条 PASS。全文件跑一遍，记下既有测试的红名单（都是"无鉴权直打 API"的旧世界断言）→ Task 6 处理。
- [ ] **Step 5: Commit** — `git commit -m "feat(auth): server 统一前置门 + setup/login/logout/status 四端点"`

### Task 6: 撤旧 token 门（per-branch × 7 + handleApiRoute）+ 既有测试改写

**Files:**
- Modify: `src/dashboard/server.ts`（删 7 处 `if (token && reqToken !== token)` 分支块）
- Modify: `src/dashboard/router.ts`（`handleApiRoute` 撤第三参 `configuredToken` 与 `req.token`；首行 401 判定删除）
- Modify: `src/dashboard/router.test.ts`、`src/dashboard/server.test.ts`

- [ ] **Step 1: server.ts 删 7 处 per-branch token 门**——reconcile-all / parked·triage claim / triage unexclude / settings PUT / settings roots / workflow redispatch / trace-stream / tmdb search 分支里的
```ts
        if (token && reqToken !== token) {
          res.writeHead(401, { 'content-type': 'application/json; charset=utf-8' })
          res.end(JSON.stringify({ error: 'unauthorized' }))
          return
        }
```
整块删除（前置门已统一拦截）。`reqToken` 变量与 `handleApiRoute(..., token)` 传参一并清理：`handleApiRoute({ pathname: rawPath, query }, deps)`。**`DashboardOpts.token` 保留**——它现在唯一的角色是 legacy 兼容输入，喂前置门。

- [ ] **Step 2: router.ts 撤门**

```ts
export function handleApiRoute(
  req: { pathname: string; query: Record<string, string> },
  deps: RouterDeps,
): ApiResult {
  const { pathname } = req
  // …其余不动（首行 configuredToken 401 判定删除）
```

- [ ] **Step 3: 测试改写**——router.test.ts 删"带错 token 401/带对 token 200"类断言（门不在这层了，改由 server.test.ts 前置门 9 条覆盖）；server.test.ts 既有直打 API 的测试全部在 `start()` 后先走一次 setup+cookie 或改用 `?token=`（最省事：给这些旧测试统一传 `start(dist, 'tok')` + 请求带 `?token=tok`——legacy 通道语义本来就要长期保活，测试顺便当兼容回归）。逐个跑绿。

- [ ] **Step 4: Run full file** — `npx vitest run src/dashboard/` 全 PASS；`npx tsc --noEmit` exit 0
- [ ] **Step 5: Commit** — `git commit -m "refactor(auth): 撤散布 token 门——前置门是唯一门（handleApiRoute 去 token 参）"`

### Task 7: fs/list 收口（R2D-2 闭环验证）

**Files:**
- Test: `src/dashboard/server.test.ts`

实现上 Task 5/6 已覆盖（fs/list 走前置门）；本 task 是**显式回归测试**把 R2D-2 钉死，防未来有人给 fs/list 开洞：

- [ ] **Step 1: Write the tests**

```ts
describe('fs/list 收口（R2D-2：未鉴权全盘枚举关闭）', () => {
  it('未初始化：?path=/etc 401 setup required（向导阶段无匿名浏览）', async () => {
    const { base } = await start(distWith('x'))
    expect((await fetch(`${base}/api/v2/fs/list?path=/etc`)).status).toBe(401)
  })
  it('已初始化未登录：401；登录后可浏览（加根选择器的正常用途）', async () => {
    const { base } = await start(distWith('x'))
    const setup = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const cookie = (setup.headers.get('set-cookie') ?? '').split(';')[0]
    expect((await fetch(`${base}/api/v2/fs/list?path=/`)).status).toBe(401)
    expect((await fetch(`${base}/api/v2/fs/list?path=/`, { headers: { cookie } })).status).toBe(200)
  })
})
```

- [ ] **Step 2: Run** — 应当直接 PASS（Task 5/6 的门已生效；若 FAIL 说明门有洞，回去修门）。这两条的价值是回归钉子，不是驱动新实现——**如果它们意外先绿，不算违反 TDD**（它们锁的是 Task 5 已红过绿过的行为）。
- [ ] **Step 3: Commit** — `git commit -m "test(auth): fs/list R2D-2 回归钉死（未鉴权枚举 401）"`

---

## A2 前端向导+登录

**web/ 实勘要点（2026-07-19 测绘）：** 手写 hash 路由（`web/src/shell/route.ts`，`Tab = 'library'|'workflow'|'triage'|'settings'`）；`App.tsx:7-13` 是产品根（`<I18nProvider><Shell/></I18nProvider>`）——**setup/login 不做成 tab，做成 App 层门**（Shell 之前整体切换，天然满足"全路由重定向"）。api client `web/src/api/client.ts`：相对路径 + `withToken()`（从页面 `?token=` 取 legacy token 附到每个请求，**保留不动**——legacy 通道要长期保活）；cookie 走 fetch 默认 same-origin，无需 `credentials` 改动；SSE（`workflow/traceStream.ts`）same-origin EventSource 自动带 cookie，登录后即通，零改动。i18n：`web/src/i18n/en.ts`+`zh.ts` 平面字典双表同步（en 是 TKey 源，漏 zh 键是类型错误）。测试：vitest+Testing Library，colocated `*.test.tsx`，`vi.stubGlobal('fetch', ...)` 模式，跑法 `cd web && npm test`。

### Task 8: api client——auth 端点 + 全局 401 事件

**Files:**
- Modify: `web/src/api/client.ts`
- Modify: `web/src/api/types.ts`
- Test: `web/src/api/client.test.ts`

- [ ] **Step 1: Write the failing tests**（追加到 `web/src/api/client.test.ts`，沿用文件里现成的 fetch stub 手法）

```tsx
describe('auth api（A2）', () => {
  it('authStatus 打 GET /api/v2/auth/status', async () => {
    const mock = vi.fn(async () => ({ ok: true, status: 200, json: async () => ({ initialized: true, authenticated: false }) }))
    vi.stubGlobal('fetch', mock)
    const r = await api.authStatus()
    expect(r).toEqual({ initialized: true, authenticated: false })
    expect(mock.mock.calls[0][0]).toBe('/api/v2/auth/status')
  })
  it('login 打 POST /api/v2/auth/login 带 JSON body', async () => {
    const mock = vi.fn(async () => ({ ok: true, status: 200, json: async () => ({ ok: true }) }))
    vi.stubGlobal('fetch', mock)
    await api.login('admin', 'pw')
    const [path, init] = mock.mock.calls[0]
    expect(path).toBe('/api/v2/auth/login')
    expect(init.method).toBe('POST')
    expect(JSON.parse(init.body)).toEqual({ username: 'admin', password: 'pw' })
  })
  it('任何请求 401 时派发 scout:unauthorized 全局事件（App 门用来切回 login）', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: false, status: 401, json: async () => ({ error: 'unauthorized' }) })))
    const seen = vi.fn()
    window.addEventListener('scout:unauthorized', seen)
    await expect(api.settings()).rejects.toThrow()
    expect(seen).toHaveBeenCalledTimes(1)
    window.removeEventListener('scout:unauthorized', seen)
  })
})
```

- [ ] **Step 2: RED** — `cd web && npm test -- client.test`：新 3 条 FAIL（api.authStatus/login 不存在、无事件派发）

- [ ] **Step 3: Implementation**

`web/src/api/types.ts` 追加：

```ts
export interface AuthStatusDTO { initialized: boolean; authenticated: boolean }
export interface AuthSecurityDTO { username: string; apiKey: string }
```

`web/src/api/client.ts`：`get`/`mutate` 两处在拿到 `res.status === 401` 时（throw 之前）加一行 `window.dispatchEvent(new Event('scout:unauthorized'))`；`errorMessage` 的 401 特判文案从"append ?token="改为登录提示（i18n 不进 client 层，保持现状英文/中文硬编码风格照旧）。`api` 对象追加：

```ts
  authStatus: () => get<AuthStatusDTO>('/api/v2/auth/status'),
  authSetup: (username: string, password: string) =>
    post<{ ok: true; apiKey: string }>('/api/v2/auth/setup', { username, password }),
  login: (username: string, password: string) => post<{ ok: true }>('/api/v2/auth/login', { username, password }),
  logout: () => post<{ ok: true }>('/api/v2/auth/logout'),
  authSecurity: () => get<AuthSecurityDTO>('/api/v2/auth/security'),
  changePassword: (oldPassword: string, newPassword: string) =>
    post<{ ok: true }>('/api/v2/auth/change-password', { oldPassword, newPassword }),
  regenerateApiKey: () => post<{ apiKey: string }>('/api/v2/auth/regenerate-api-key'),
```

（`post` 是文件里现成的 helper；`/api/v2/auth/security`、`change-password`、`regenerate-api-key` 三端点在 Task 12 服务端补——本 task 只加客户端函数，测试只打 stub 不打真端点，顺序无碍。）

- [ ] **Step 4: GREEN** — `npm test -- client.test` 全 PASS
- [ ] **Step 5: Commit** — `git commit -m "feat(web/auth): api client auth 端点 + 全局 401 事件"`

### Task 9: SetupWizard 页

**Files:**
- Create: `web/src/auth/SetupWizard.tsx`
- Test: `web/src/auth/SetupWizard.test.tsx`

- [ ] **Step 1: Write the failing tests**

```tsx
// web/src/auth/SetupWizard.test.tsx
import { describe, it, expect, afterEach, vi } from 'vitest'
import { render, screen, fireEvent, waitFor, cleanup } from '@testing-library/react'
import { I18nProvider } from '../i18n/useT.js'
import { SetupWizard } from './SetupWizard.js'

afterEach(() => { cleanup(); vi.restoreAllMocks(); vi.unstubAllGlobals() })

function renderWizard(onDone = vi.fn()) {
  render(<I18nProvider><SetupWizard onDone={onDone} /></I18nProvider>)
  return onDone
}

describe('SetupWizard（A2：首启向导）', () => {
  it('渲染用户名/密码/确认三输入与提交钮', () => {
    renderWizard()
    expect(screen.getByLabelText(/username|用户名/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/^password|^密码/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/confirm|确认/i)).toBeInTheDocument()
  })
  it('两次密码不一致 → 前端拦截显示错误，不发请求', async () => {
    const mock = vi.fn()
    vi.stubGlobal('fetch', mock)
    renderWizard()
    fireEvent.change(screen.getByLabelText(/username|用户名/i), { target: { value: 'admin' } })
    fireEvent.change(screen.getByLabelText(/^password|^密码/i), { target: { value: 'hunter2222' } })
    fireEvent.change(screen.getByLabelText(/confirm|确认/i), { target: { value: 'different' } })
    fireEvent.click(screen.getByRole('button'))
    await screen.findByText(/not match|不一致/i)
    expect(mock).not.toHaveBeenCalled()
  })
  it('提交成功 → 展示 apiKey 一次性告知并调 onDone', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: true, status: 200, json: async () => ({ ok: true, apiKey: 'a'.repeat(32) }) })))
    const onDone = renderWizard()
    fireEvent.change(screen.getByLabelText(/username|用户名/i), { target: { value: 'admin' } })
    fireEvent.change(screen.getByLabelText(/^password|^密码/i), { target: { value: 'hunter2222' } })
    fireEvent.change(screen.getByLabelText(/confirm|确认/i), { target: { value: 'hunter2222' } })
    fireEvent.click(screen.getByRole('button'))
    await screen.findByText('a'.repeat(32))
    fireEvent.click(screen.getByRole('button', { name: /enter|进入/i }))
    expect(onDone).toHaveBeenCalled()
  })
  it('服务端 400（密码太短等）→ 错误行内展示', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: false, status: 400, json: async () => ({ error: 'password must be at least 8 characters' }) })))
    renderWizard()
    fireEvent.change(screen.getByLabelText(/username|用户名/i), { target: { value: 'admin' } })
    fireEvent.change(screen.getByLabelText(/^password|^密码/i), { target: { value: 'short' } })
    fireEvent.change(screen.getByLabelText(/confirm|确认/i), { target: { value: 'short' } })
    fireEvent.click(screen.getByRole('button'))
    await screen.findByText(/at least 8/i)
  })
})
```

- [ ] **Step 2: RED** — 模块不存在 FAIL

- [ ] **Step 3: Implementation** — `web/src/auth/SetupWizard.tsx`。视觉遵 DESIGN.md：居中窄卡（`settings-section` 同款 class 语汇），标题可用 serif（spec §3 授权的"只此一处仪式感"——setup/login 与 Shell 永不同屏，不与库空态 serif 冲突，实现时先 grep 确认 login/setup 屏内无第二处 serif）；lime 强调至多一次（提交钮）。三态齐全（提交中 disabled/错误行内 ≤6% tint）。骨架：

```tsx
import { useState } from 'react'
import { useT } from '../i18n/useT.js'
import { api } from '../api/client.js'

export function SetupWizard({ onDone }: { onDone: () => void }) {
  const { t } = useT()
  const [username, setUsername] = useState('')
  const [password, setPassword] = useState('')
  const [confirm, setConfirm] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [busy, setBusy] = useState(false)
  const [apiKey, setApiKey] = useState<string | null>(null)

  async function submit(e: React.FormEvent) {
    e.preventDefault()
    setError(null)
    if (password !== confirm) { setError(t('setup_password_mismatch')); return }
    setBusy(true)
    try {
      const r = await api.authSetup(username, password)
      setApiKey(r.apiKey)   // 一次性告知页：唯一一次全量展示，抄走后进入
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e))
    } finally {
      setBusy(false)
    }
  }

  if (apiKey !== null) {
    return ( /* apiKey 一次性展示 + t('setup_enter_label') 按钮 → onDone() */ )
  }
  return ( /* <form onSubmit={submit}> 三输入（label htmlFor 关联，供测试 getByLabelText）+ 错误行 + 提交钮 */ )
}
```

（两个 return 的 JSX 按 DESIGN.md 语汇写全——输入用 Astryx 组件或原生 input + 现有 settings 输入样式类，实现时对照 `BehaviorSection.tsx` 的输入写法。）

- [ ] **Step 4: GREEN**；**Step 5: Commit** — `git commit -m "feat(web/auth): 首启向导页（三输入+一次性 apiKey 告知）"`

### Task 10: LoginPage 页

**Files:**
- Create: `web/src/auth/LoginPage.tsx`
- Test: `web/src/auth/LoginPage.test.tsx`

- [ ] **Step 1: Write the failing tests**

```tsx
// web/src/auth/LoginPage.test.tsx（import/afterEach 同 SetupWizard.test.tsx）
describe('LoginPage（A2）', () => {
  it('提交成功调 onDone', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: true, status: 200, json: async () => ({ ok: true }) })))
    const onDone = vi.fn()
    render(<I18nProvider><LoginPage onDone={onDone} /></I18nProvider>)
    fireEvent.change(screen.getByLabelText(/username|用户名/i), { target: { value: 'admin' } })
    fireEvent.change(screen.getByLabelText(/password|密码/i), { target: { value: 'hunter2222' } })
    fireEvent.click(screen.getByRole('button'))
    await waitFor(() => expect(onDone).toHaveBeenCalled())
  })
  it('401 错误行内展示（invalid username or password）', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: false, status: 401, json: async () => ({ error: 'invalid username or password' }) })))
    render(<I18nProvider><LoginPage onDone={vi.fn()} /></I18nProvider>)
    fireEvent.change(screen.getByLabelText(/username|用户名/i), { target: { value: 'admin' } })
    fireEvent.change(screen.getByLabelText(/password|密码/i), { target: { value: 'wrong' } })
    fireEvent.click(screen.getByRole('button'))
    await screen.findByText(/invalid username/i)
  })
})
```

- [ ] **Step 2: RED**；**Step 3: Implementation** — 结构=SetupWizard 减确认框减 apiKey 页（`api.login` 成功 → `onDone()`；失败 setError 行内展示）。极简（spec §3）。
- [ ] **Step 4: GREEN**；**Step 5: Commit** — `git commit -m "feat(web/auth): 登录页"`

### Task 11: App 层鉴权门（未初始化→向导；未登录→login；401 事件→重探）

**Files:**
- Modify: `web/src/App.tsx`
- Create: `web/src/auth/useAuthStatus.ts`
- Test: `web/src/App.test.tsx`（追加）

- [ ] **Step 1: Write the failing tests**（`App.test.tsx` 现有测试基座上追加；fetch stub 需按 URL 分流——auth/status 之外的请求给通用 200 空数据，照文件内现有 stub 手法扩）

```tsx
describe('App 鉴权门（A2）', () => {
  it('auth/status initialized:false → 渲染 SetupWizard（不渲染 Shell）', async () => {
    stubFetchAuthStatus({ initialized: false, authenticated: false })
    render(<App />)
    await screen.findByText(/create admin|创建管理员/i)
    expect(screen.queryByText(/LIBRARY/)).not.toBeInTheDocument()
  })
  it('initialized:true authenticated:false → 渲染 LoginPage', async () => {
    stubFetchAuthStatus({ initialized: true, authenticated: false })
    render(<App />)
    await screen.findByRole('button', { name: /log in|登录/i })
  })
  it('authenticated:true → 渲染 Shell', async () => {
    stubFetchAuthStatus({ initialized: true, authenticated: true })
    render(<App />)
    await screen.findByText(/LIBRARY/)
  })
})
```

（`stubFetchAuthStatus`：`vi.stubGlobal('fetch', vi.fn(async (url) => url === '/api/v2/auth/status' ? okJson(status) : okJson(/* 该 url 对应的最小空数据 */)))`——按 App.test.tsx 现有写法落地。）

- [ ] **Step 2: RED**；**Step 3: Implementation**

```tsx
// web/src/auth/useAuthStatus.ts
import { useCallback, useEffect, useState } from 'react'
import { api } from '../api/client.js'
import type { AuthStatusDTO } from '../api/types.js'

/** App 层鉴权门的数据源：mount 探一次 /auth/status；收到全局 scout:unauthorized（任意请求 401）
 *  即重探——会话过期/登出后 App 自动切回 login，无需每个 hook 各自处理 401。 */
export function useAuthStatus() {
  const [status, setStatus] = useState<AuthStatusDTO | null>(null)
  const reload = useCallback(() => {
    api.authStatus().then(setStatus).catch(() => setStatus(null))
  }, [])
  useEffect(() => {
    reload()
    window.addEventListener('scout:unauthorized', reload)
    return () => window.removeEventListener('scout:unauthorized', reload)
  }, [reload])
  return { status, reload }
}
```

```tsx
// web/src/App.tsx
import { I18nProvider } from './i18n/useT.js'
import { Shell } from './shell/AppShell.js'
import { SetupWizard } from './auth/SetupWizard.js'
import { LoginPage } from './auth/LoginPage.js'
import { useAuthStatus } from './auth/useAuthStatus.js'

function AuthGate() {
  const { status, reload } = useAuthStatus()
  if (status === null) return null          // 探测中/探测失败：空白一拍（<100ms），不闪错误
  if (!status.initialized) return <SetupWizard onDone={reload} />
  if (!status.authenticated) return <LoginPage onDone={reload} />
  return <Shell />
}

export function App() {
  return (
    <I18nProvider>
      <AuthGate />
    </I18nProvider>
  )
}
```

i18n：en.ts/zh.ts 双表同步补 `setup_*`/`login_*` 全部键（`setup_heading: 'Create admin account'/'创建管理员账号'`、`setup_password_mismatch`、`setup_apikey_notice`、`setup_enter_label`、`login_heading`、`login_submit`、`auth_username_label`、`auth_password_label`、`auth_confirm_label` 等——以两页实现里实际用到的 t() 键为准，en 定名，zh 对译）。

- [ ] **Step 4: GREEN** — `cd web && npm test` 全量 PASS（App.test.tsx 既有测试若因缺 auth/status stub 变红，统一给它们的 fetch stub 补一条 authenticated:true 分流——旧测试语义不变）
- [ ] **Step 5: Commit** — `git commit -m "feat(web/auth): App 层鉴权门——setup/login/Shell 三态 + 401 全局重探"`

---

## A2′ 设计打磨（高星仓调研 → agency 设计人格审计后追加，2026-07-19 夜）

调研 `docs`（scratchpad/design-recon-auth-pages.md，源自 Sonarr/jellyfin-web/Overseerr 真源码 + issue 血泪）。K3 的 Task 8-10 先落**功能骨架**（api client + 两页 + 测试红绿），本段在其之上做**结构/无障碍/文案打磨**，每项 TDD。核心裁决：**"让用户重打刚建的凭据"是被调研对象普遍的最烂共性**——我们的 setup 已定案 auto-login，天然避开；其余强建议如下。

### Task 8′: 共享 AuthShell（全站唯一 serif 落此一处）

**Files:** Create `web/src/auth/AuthShell.tsx` + test；Modify SetupWizard/LoginPage/(apikey 屏) 复用它。

- [ ] **RED**：AuthShell 测试——渲染 `heading` prop 于 `<h1>`；`<h1>` 命中 serif class（`getComputedStyle` 在 jsdom 不可靠→改断言 className 含约定的 serif token 类，或 DESIGN 里 serif 的具体 class 名，实现时 grep web/src 找现有 serif 用法对齐）；children 渲染在标题下。
- [ ] **GREEN**：`AuthShell.tsx`——居中窄列（~360px）裸画布 `#0b0c0f`；小 wordmark→serif `<h1>`（28-32px）→children。SetupWizard/LoginPage/apikey 屏各自把标题文案传 `heading` prop、表单作 children。
  - **预研已确认（2026-07-19，主控只读勘查）**：web/src 当前 **serif 零使用**（全站只有 `Geist Mono`，DESIGN §3/§4 那"唯一一次衬线"名额**尚未动用**）——AuthShell 合法认领它。但**没有任何 serif 字体被加载**（scout.ts 只定义了 code:Geist Mono，无 heading serif；styles.css 无 @font-face serif）。落地铁律：**用 web-safe serif 栈**（`Georgia, 'Times New Roman', serif`），**绝不引 webfont**（自托管离线形态 + 无新依赖）。serif 只加在 AuthShell 的 `<h1>`——用 `xstyle`/className 就地给 font-family，不进 scout.ts 全局（避免污染，且它只服务这一个组件）。
  - 动手后 grep `web/src` 复查 serif/Georgia 仅 AuthShell 一处。
- [ ] Commit `feat(web/auth): 共享 AuthShell——全站唯一 serif 的仪式感落此`

### Task 9′: 密码管理器契约 + show-password 切换 + 上手期长度提示

**Files:** Modify SetupWizard.tsx / LoginPage.tsx（+ 各自测试）。

- [ ] **RED**（追加断言）：
  - SetupWizard：username input `autocomplete="username"`；password/confirm 均 `autocomplete="new-password"`；有 show-password 切换钮，点击后 password input `type` 从 `password` 变 `text`。
  - LoginPage：username `autocomplete="username"`；password `autocomplete="current-password"`；`<form>` 真实提交（已具备）。
  - 长度提示：SetupWizard password 下有一行静态 mono 提示（`min 10 characters` 或 i18n 键），满足时从 muted 翻 normal（断言提示元素存在 + 满足态 class 切换）。
  ```tsx
  // SetupWizard.test.tsx 追加
  it('密码字段带 new-password autocomplete + show-password 切换', () => {
    renderWizard()
    const pw = screen.getByLabelText(/^password|^密码/i)
    expect(pw).toHaveAttribute('autocomplete', 'new-password')
    expect(pw).toHaveAttribute('type', 'password')
    fireEvent.click(screen.getByRole('button', { name: /show|显示/i }))
    expect(pw).toHaveAttribute('type', 'text')
  })
  it('username 带 username autocomplete', () => {
    renderWizard()
    expect(screen.getByLabelText(/username|用户名/i)).toHaveAttribute('autocomplete', 'username')
  })
  ```
  ```tsx
  // LoginPage.test.tsx 追加
  it('密码管理器契约:username=username / password=current-password', () => {
    render(<I18nProvider><LoginPage onDone={vi.fn()} /></I18nProvider>)
    expect(screen.getByLabelText(/username|用户名/i)).toHaveAttribute('autocomplete', 'username')
    expect(screen.getByLabelText(/password|密码/i)).toHaveAttribute('autocomplete', 'current-password')
  })
  ```
- [ ] **GREEN**：加 autocomplete 属性；show-password 用一个 `useState<boolean>` 切 `type`；长度提示 `<span className={pw.length>=10?normal:muted}>`（mono）。**⚠️禁止**任何 `autocomplete="off"` 或 `data-1pignore/lpignore/bwignore`（Overseerr 的组件复用臭味——那些属于 api-key 输入，绝不上登录密码框）。
- [ ] Commit `feat(web/auth): 密码管理器契约 + show-password + 上手期长度提示`

### Task 10′: LoginPage 错误行文案 + username 保留 + 诚实找回

**Files:** Modify LoginPage.tsx（+ 测试）。

- [ ] **RED**：
  - 401 时错误行文案恰为 `Incorrect username or password.`（i18n）——不吞成 Overseerr 式"Something went wrong"；传输失败（fetch reject 非 401）另一变体 `Can't reach the server.`。
  - 提交失败后 username input 值**保留**、password input **清空**、焦点回到 password。
  - 页面底一行 muted：找回密码走 CLI，命令 mono 展示（**文案见 Task 15′——命令必须真实存在**）。
  ```tsx
  it('401 后保留 username、清空 password、错误文案精确', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: false, status: 401, json: async () => ({ error: 'invalid username or password' }) })))
    render(<I18nProvider><LoginPage onDone={vi.fn()} /></I18nProvider>)
    const u = screen.getByLabelText(/username|用户名/i) as HTMLInputElement
    const p = screen.getByLabelText(/password|密码/i) as HTMLInputElement
    fireEvent.change(u, { target: { value: 'admin' } })
    fireEvent.change(p, { target: { value: 'wrong-pass' } })
    fireEvent.click(screen.getByRole('button', { name: /log in|登录|sign in/i }))
    await screen.findByText(/incorrect username or password/i)
    expect(u.value).toBe('admin')   // 保留
    expect(p.value).toBe('')        // 清空
  })
  ```
- [ ] **GREEN**：错误分支按 status 分文案；成功/失败后只 setPassword('')，username 状态不动；`passwordRef.current?.focus()`。
- [ ] Commit `feat(web/auth): 登录错误行文案精确化 + username 保留 + 诚实找回提示`

（**结构性已白赚的两个 *arr bug**：#6144 stale-cookie 死循环——我们 AuthGate 每次读 auth/status，cookie 失效即 authenticated:false→LoginPage，无循环；#6454 deep-link 丢失——AuthGate 包裹 Shell、从不改 location.hash，登录后 reload() 直接在原 hash 渲染 Shell，深链天然保留。两者无需额外代码，测试各补一条钉死即可，放 Task 11 App 门测试里。）

## A3 Settings Security 区

### Task 12: 服务端三端点（security/change-password/regenerate-api-key）

**Files:**
- Modify: `src/dashboard/server.ts`
- Test: `src/dashboard/server.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
describe('auth Security 区端点（A3）', () => {
  async function setupAndCookie(base: string) {
    const r = await fetch(`${base}/api/v2/auth/setup`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password: 'hunter2222' }),
    })
    const { apiKey } = await r.json() as { apiKey: string }
    return { cookie: (r.headers.get('set-cookie') ?? '').split(';')[0], apiKey }
  }
  it('GET /api/v2/auth/security：authed 才给 username+完整 apiKey', async () => {
    const { base } = await start(distWith('x'))
    const { cookie, apiKey } = await setupAndCookie(base)
    expect((await fetch(`${base}/api/v2/auth/security`)).status).toBe(401)
    const r = await fetch(`${base}/api/v2/auth/security`, { headers: { cookie } })
    expect(r.status).toBe(200)
    expect(await r.json()).toEqual({ username: 'admin', apiKey })
  })
  it('POST change-password：旧密码错 400；对则 200 且新密码能登、旧的不能', async () => {
    const { base } = await start(distWith('x'))
    const { cookie } = await setupAndCookie(base)
    const post = (body: unknown) => fetch(`${base}/api/v2/auth/change-password`, {
      method: 'POST', headers: { 'content-type': 'application/json', cookie }, body: JSON.stringify(body),
    })
    expect((await post({ oldPassword: 'wrong', newPassword: 'newpass888' })).status).toBe(400)
    expect((await post({ oldPassword: 'hunter2222', newPassword: 'newpass888' })).status).toBe(200)
    const login = (password: string) => fetch(`${base}/api/v2/auth/login`, {
      method: 'POST', headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ username: 'admin', password }),
    })
    expect((await login('hunter2222')).status).toBe(401)
    expect((await login('newpass888')).status).toBe(200)
  })
  it('POST regenerate-api-key：返回新 key，旧 key 立即失效', async () => {
    const { base } = await start(distWith('x'))
    const { cookie, apiKey } = await setupAndCookie(base)
    const r = await fetch(`${base}/api/v2/auth/regenerate-api-key`, { method: 'POST', headers: { cookie } })
    const { apiKey: nk } = await r.json() as { apiKey: string }
    expect(nk).toMatch(/^[0-9a-f]{32}$/)
    expect((await fetch(`${base}/api/v2/library`, { headers: { 'x-api-key': apiKey } })).status).toBe(401)
    expect((await fetch(`${base}/api/v2/library`, { headers: { 'x-api-key': nk } })).status).toBe(200)
  })
})
```

- [ ] **Step 2: RED**（三端点 404→被前置门 401 拦也算——断言 status 精确值会红）

- [ ] **Step 3: Implementation** — server.ts 前置门**之后**（即已鉴权区域）加三个分支（位置：`if (!authed)` 拦截块之后、reconcile-all 分支之前）：

```ts
      // ---- 鉴权 A3：Security 区三端点（已过前置门，此处必然 authed）----
      if (rawPath === '/api/v2/auth/security') {
        if (req.method !== 'GET') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
        // 完整 apiKey 只对已鉴权管理员回显（Sonarr 同款语义）；前端脱敏展示尾 4 位、复制钮拷全量
        res.writeHead(200, JSON_CT)
        res.end(JSON.stringify({ username: settingsRepo.get('auth_username') ?? '', apiKey: settingsRepo.get('auth_api_key') ?? '' }))
        return
      }
      if (rawPath === '/api/v2/auth/change-password') {
        if (req.method !== 'POST') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
        const body = await readJsonBody(req)
        if (body === undefined) { res.writeHead(400, JSON_CT); res.end(JSON.stringify({ error: 'invalid JSON body' })); return }
        const b = (body ?? {}) as { oldPassword?: unknown; newPassword?: unknown }
        const r = auth.changePassword(
          typeof b.oldPassword === 'string' ? b.oldPassword : '',
          typeof b.newPassword === 'string' ? b.newPassword : '', Date.now(),
        )
        res.writeHead(r.ok ? 200 : 400, JSON_CT)
        res.end(JSON.stringify(r.ok ? { ok: true } : { error: r.error }))
        return
      }
      if (rawPath === '/api/v2/auth/regenerate-api-key') {
        if (req.method !== 'POST') { res.writeHead(405, JSON_CT); res.end(JSON.stringify({ error: 'method not allowed' })); return }
        res.writeHead(200, JSON_CT)
        res.end(JSON.stringify({ apiKey: auth.regenerateApiKey(Date.now()) }))
        return
      }
```

（`AUTH_KEYS` 字面量 `'auth_username'`/`'auth_api_key'` 这里直接经 settingsRepo 读——或从 auth.ts import AUTH_KEYS 用常量，取后者，防字符串漂移。）

- [ ] **Step 4: GREEN**；**Step 5: Commit** — `git commit -m "feat(auth): Security 区三端点（security/改密/重生成 key）"`

### Task 13: SecuritySection 组件

**Files:**
- Create: `web/src/settings/SecuritySection.tsx`
- Modify: `web/src/settings/SettingsPage.tsx`
- Test: `web/src/settings/SecuritySection.test.tsx`

- [ ] **Step 1: Write the failing tests**（照 `BehaviorSection.test.tsx` 的 I18nProvider+stub fetch 模式）

```tsx
describe('SecuritySection（A3）', () => {
  it('展示 username 与脱敏 api key（****+尾4位），有复制与重生成钮', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({ ok: true, status: 200, json: async () => ({ username: 'admin', apiKey: 'a'.repeat(28) + 'beef' }) })))
    render(<I18nProvider><SecuritySection /></I18nProvider>)
    await screen.findByText('admin')
    expect(screen.getByText(/beef$/)).toBeInTheDocument()
    expect(screen.queryByText('a'.repeat(28) + 'beef')).not.toBeInTheDocument() // 不整串明示
    expect(screen.getByRole('button', { name: /copy|复制/i })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /regenerate|重新生成/i })).toBeInTheDocument()
  })
  it('改密表单：新旧密码提交 → POST change-password；400 错误行内展示', async () => {
    const mock = vi.fn(async (url: string) =>
      url === '/api/v2/auth/security'
        ? { ok: true, status: 200, json: async () => ({ username: 'admin', apiKey: 'k'.repeat(32) }) }
        : { ok: false, status: 400, json: async () => ({ error: 'current password is incorrect' }) })
    vi.stubGlobal('fetch', mock)
    render(<I18nProvider><SecuritySection /></I18nProvider>)
    await screen.findByText('admin')
    fireEvent.change(screen.getByLabelText(/current password|当前密码/i), { target: { value: 'old' } })
    fireEvent.change(screen.getByLabelText(/new password|新密码/i), { target: { value: 'newpass888' } })
    fireEvent.click(screen.getByRole('button', { name: /change password|修改密码/i }))
    await screen.findByText(/incorrect/i)
  })
  it('重生成：确认后 POST regenerate-api-key，新尾 4 位上屏', async () => {
    let regenerated = false
    vi.stubGlobal('fetch', vi.fn(async (url: string, init?: RequestInit) => {
      if (url === '/api/v2/auth/regenerate-api-key') { regenerated = true; return { ok: true, status: 200, json: async () => ({ apiKey: 'b'.repeat(28) + 'cafe' }) } }
      return { ok: true, status: 200, json: async () => ({ username: 'admin', apiKey: 'a'.repeat(28) + 'beef' }) }
    }))
    vi.stubGlobal('confirm', vi.fn(() => true))  // 破坏性动作要确认（旧 key 立即失效）
    render(<I18nProvider><SecuritySection /></I18nProvider>)
    await screen.findByText(/beef$/)
    fireEvent.click(screen.getByRole('button', { name: /regenerate|重新生成/i }))
    await screen.findByText(/cafe$/)
    expect(regenerated).toBe(true)
  })
})
```

- [ ] **Step 2: RED**；**Step 3: Implementation** — `SecuritySection.tsx`：`useEffect` mount 拉 `api.authSecurity()` 存本地 state（不进全局 hooks——只有这区用）；脱敏 `'••••••••' + apiKey.slice(-4)` mono 展示；复制钮 `navigator.clipboard.writeText(apiKey)` 后 label 闪 "Copied"；重生成钮 `confirm(t('settings_security_regen_confirm'))` 后调 `api.regenerateApiKey()` 更新本地。**regenerate 确认文案必须陈述爆炸半径**（调研：*arr 的"Are you sure?"是无后果的坏范例）——`settings_security_regen_confirm` = "重新生成 API key？当前 key 会立即失效，任何正在使用它的客户端都将失败，直到更新为新 key。"（en 对译）。改密三输入（当前 `autocomplete=current-password`/新 `new-password`/确认 `new-password`）+ `api.changePassword`，成功清空表单 + 成功行 + 长度提示同 setup（min 10）。三态齐全。`SettingsPage.tsx` 的 VStack 里加 `<SecuritySection />`（放最后——安全区是低频操作，排 Behavior/Deploy/Roots 之后）。i18n 双表补 `settings_security_*` 键。
- [ ] **Step 4: GREEN** — `cd web && npm test` 全 PASS
- [ ] **Step 5: Commit** — `git commit -m "feat(web/auth): Settings Security 区（改密+api key 脱敏/复制/重生成）"`

### Task 14: 登出入口

**Files:**
- Modify: `web/src/shell/AppShell.tsx`（侧栏尾部或顶栏加 logout 钮——对照现有 shell 结构择位，DESIGN 语汇：mono 小字链接级，不抢视觉）
- Test: `web/src/App.test.tsx`

- [ ] **Step 1: RED test** — authenticated 态渲染 Shell 后可见 logout 钮；点击 → `POST /api/v2/auth/logout` 发出 + 重新渲染 LoginPage（stub fetch：logout 后 auth/status 改答 authenticated:false）。
- [ ] **Step 2: Implementation** — Shell 接 `onLogout` prop（App 传 `() => api.logout().then(reload)`）或 Shell 内直接调 api.logout + 派发 `scout:unauthorized`（取后者——少穿一层 prop，401 重探链路本来就在）。
- [ ] **Step 3: GREEN + Commit** — `git commit -m "feat(web/auth): 登出入口"`

---

## A4 兼容与验收

### Task 14′: A1 硬化（调研回补——长度阈值 + setup 原子性）

**Files:** Modify `src/dashboard/auth.ts` + `src/dashboard/auth.test.ts`。

- [ ] **RED**：
  - `MIN_PASSWORD_LEN` 8→**10**（Homarr 是唯一有强度规则的调研对象，且上手期声明；NIST 对齐"只管长度不管组成"；10 与 setup 页 mono 提示 `min 10 characters` 一致）。测试：9 字符 setup/changePassword → ok:false，10 字符 → ok:true。（既有测试凭据 `hunter2222`/`newpass888` 皆 10 字符，不受影响。）
  - **setup 原子性**（Jellyfin CVE 教训延伸）：三键写入若中途崩溃（写了 passwordHash 未写 apiKey），`isInitialized`（看 passwordHash）已 true 但 apiKey 为 null → verifyApiKey 永假。用 `settingsRepo.db.transaction` 包住三键写入。测试：mock 一个在第 3 次 set 抛错的 settings，setup 抛错后 `isInitialized()` 仍为 false（事务回滚，三键全无）。
  ```ts
  it('MIN_PASSWORD_LEN=10:9 字符拒绝,10 字符通过', () => {
    const auth = new AuthService(new SettingsRepo(openDb(':memory:')))
    expect(auth.setup('admin', 'x'.repeat(9), 1).ok).toBe(false)
    expect(auth.setup('admin', 'x'.repeat(10), 1).ok).toBe(true)
  })
  it('setup 原子:三键写入中途抛错→事务回滚,isInitialized 仍 false', () => {
    const db = openDb(':memory:')
    const repo = new SettingsRepo(db)
    let calls = 0
    const orig = repo.set.bind(repo)
    repo.set = (k, v, n) => { if (++calls === 3) throw new Error('disk full'); return orig(k, v, n) }
    const auth = new AuthService(repo)
    expect(() => auth.setup('admin', 'hunter2222', 1)).toThrow()
    // 新实例读同一 db:三键应因回滚而全不存在
    expect(new AuthService(new SettingsRepo(db)).isInitialized()).toBe(false)
  })
  ```
- [ ] **GREEN**：`MIN_PASSWORD_LEN = 10`；`setup` 里三次 `this.settings.set` 包进 `this.settings.db.transaction(() => {...})()`（SettingsRepo 暴露 `readonly db`，已具备）。changePassword 的 8→10 同步。
- [ ] **同步波及**：`src/dashboard/server.test.ts` 的 setup body 若有 <10 字符密码需订正（既有用 `hunter2222` 10 位，应无碍——跑一遍确认）。
- [ ] Commit `fix(auth): 密码阈值 10 + setup 三键写入原子事务（调研回补 + Jellyfin CVE 延伸）`

### Task 15: CLI 启动播报三态 + doctor + `auth reset`（诚实找回密码）

**Files:**
- Modify: `src/cli/dashboardTokenWarning.ts`（及其测试）
- Modify: `src/cli/index.ts:544-551`

- [ ] **Step 1: RED tests**（dashboardTokenWarning 现测试文件里改写）——新三态：
  - 未初始化 + 无 DASHBOARD_TOKEN：不再是 R2D-5 高声裸奔告警，改为一行指路："dashboard 未设账号——首次访问将进入创建管理员向导"（向导本身就是门，裸奔态已不存在）；
  - DASHBOARD_TOKEN 已设（无论初始化否）："DASHBOARD_TOKEN 仍有效（等价 api key）；建议在 dashboard 完成账号设置后移除该变量"；
  - 已初始化 + 无 token：零输出（健康态不聒噪）。
  函数签名改 `dashboardAuthStartupLines(opts: { tokenSet: boolean; initialized: boolean }): string[]`；index.ts 调用处传 `{ tokenSet: Boolean(process.env.DASHBOARD_TOKEN), initialized: new SettingsRepo(db).get('auth_password_hash') !== null }`（db 已在作用域）。`console.log('dashboard on ...')` 行的 `(token required)` 后缀同步改为按三态给 `(legacy token)` / `(setup pending)` / 空。
- [ ] **Step 2: GREEN + tsc + Commit** — `git commit -m "feat(auth): 启动播报三态——裸奔告警退役（R2D-5 闭环）"`

- [ ] **Step 3: `auth reset` CLI 子命令（诚实找回密码的后端）** — LoginPage/README 都要指向一条**真实存在**的找回命令（DESIGN §8 数据诚实：绝不展示不存在的命令）。加 `AuthService.reset(now)`：删三键（`settingsRepo.delete(AUTH_KEYS.username/passwordHash/apiKey)`）→ 回到未初始化态 → 下次访问重进向导。CLI 侧 `src/cli/index.ts` 认 `auth reset` 子命令（复用已打开的 db 构造 SettingsRepo+AuthService，调 reset，打印"已清除管理员凭据，下次访问 dashboard 将重新进入设置向导"）。TDD：auth.test.ts 加 `reset 后 isInitialized false + 旧 apiKey/密码全失效`；cli 侧最小 spawn 测或纯函数化 handler 单测（对齐 cli/index.ts 现有子命令测试手法，实现时 grep `cmdDoctor`/`parseArgs` 找模式）。
  - Commit `feat(auth): auth reset CLI 子命令——诚实的自托管找回密码路径`

### Task 16: README + .env.example 兼容注记

**Files:**
- Modify: `README.md`（鉴权段：首启向导、api key 用法 `X-Api-Key`/`?apikey=`、DASHBOARD_TOKEN 迁移建议、无内建 HTTPS——反代加 TLS 示例一行 caddy/nginx）
- Modify: `.env.example`（DASHBOARD_TOKEN 注释改"legacy：已有部署继续有效；新部署建议留空，用首启向导设账号"）

- [ ] **Step 1: 改写两文件**（无测试——文档任务）；**Step 2: Commit** — `git commit -m "docs(auth): README 鉴权说明 + env legacy 注记"`

### Task 17: 验收清单（spec 验收口径逐条核）

- [ ] 全量：根 `npx vitest run` + `npx tsc --noEmit` + `cd web && npm test` 三绿
- [ ] 手动/脚本核（本地起 daemon + dashboard，`:memory:` 不行——用临时 db 文件）：
  1. 未初始化首访 `http://localhost:<port>` → 向导页；
  2. 设置后开隐身窗未登录：`curl /api/v2/library` 401；SSE `curl /api/v2/workflow/trace-stream` 401；
  3. 登录后四 tab 正常 + Workflow 直播流（SSE cookie 通道）通；
  4. `curl -H 'X-Api-Key: <key>' /api/v2/library` 200；`curl '/api/v2/library?apikey=<key>'` 200；
  5. `DASHBOARD_TOKEN=x` 起服 → `curl '/api/v2/library?token=x'` 200（未初始化也通——旧部署零破坏）；
  6. fs/list 未鉴权 401（R2D-2 死透）。
- [ ] 生产部署走既有 deploy 协议（git archive → build → 备 DB → up），部署后跑第 1-6 条生产版
- [ ] **Commit**（若有修补）+ 汇报

---

## 调研折入记录（2026-07-19 夜，敲骨吸髓自 Sonarr/jellyfin-web/Overseerr 真源码）

强建议 → 落点映射（全文 scratchpad/design-recon-auth-pages.md）：
- 单屏建管理员 + **auto-login**（避开 *arr/Jellyfin 最烂共性"重打刚建凭据"）→ 已在 A1 Task 5 setup 分支（成功即签 cookie）✅
- **server-side 单次 gate**（Jellyfin `/wizardstart.html` 未授权改密 CVE）→ A1 已有 isInitialized 403；**原子性**补进 Task 14′
- **AuthShell 共享 serif**（Overseerr backdrop 的暗色极简等价物，零交互成本的人格）→ Task 8′
- **密码管理器契约**（autocomplete username/current-password/new-password，禁 off、禁 data-*ignore）→ Task 9′
- **show-password 切换 + 上手期长度提示**（Homarr 的透明，不要它的组成规则）→ Task 9′ + 阈值 10 进 Task 14′
- **登录错误精确文案 + username 保留 + password 清空聚焦**（避 Overseerr "Something went wrong" / *arr `?loginFailed=true` reload）→ Task 10′
- **诚实找回密码**（*arr/Homarr 的 CLI 恢复）→ `auth reset` CLI（Task 15 Step 3）背书 LoginPage 文案
- **api key 一次性全显 + 脱敏 tail-4 + regenerate 陈述爆炸半径 + 即时生效无需重启**（避 *arr 明文常驻/重启才生效/无后果确认）→ b 屏 + A3 Task 13
- 结构性白赚：#6144 stale-cookie 循环、#6454 deep-link 丢失——AuthGate 包裹架构天然免疫，Task 11 补钉

## Self-Review 记录

- **Spec coverage:** §1 三键=Task 4；§2 统一门/兼容/节流/SSE=Task 5-7；§3 向导/登录/401 跳转/Security 区/双语=Task 8-14；§4 非目标未越界（无多用户/OIDC/HTTPS）；验收口径=Task 17 逐条映射。R2D-2=Task 7+17.6；R2D-5=Task 15。
- **Placeholder scan:** Task 9/13 的 JSX 细节留了"按 DESIGN.md 语汇写全"的实现自由度（视觉层无法在计划里逐 px 写死，属合理边界——数据流/状态机/断言全部落全码）；无 TBD/TODO。
- **Type consistency:** `AuthService.setup→SetupResult{ok,apiKey}`、`login→LoginResult{ok,sessionToken|status}` 在 Task 4 定义、Task 5/12 服务端与 Task 8 客户端 DTO 一致；cookie 名 `scout_session` 全文统一；api key 32 hex（`randomBytes(16)`）全文统一。
- **兼容红线：** `DashboardOpts.token` 不删；`withToken` 不删；legacy `?token=`/`x-dashboard-token` 在前置门长期等价 api key。
