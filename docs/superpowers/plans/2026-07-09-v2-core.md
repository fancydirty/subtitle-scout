# v2 一期（内核）实现计划：SQLite 状态机 + 调和循环

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 SQLite 单库状态机 + 按剧调和循环替换 v1 的 queue/watcher 状态层，判断链原样复用。

**Architecture:** 新增 `src/v2/`（db / repos / scanner / aggregator / executor / daemon），v1 判断链
（pipeline/planSearch/rank/seasonPack/subtitleWriter）零改动复用；执行器经 `PipelineDeps.seasonPack.onCovered`
把逐集战果落库。spec：`docs/superpowers/specs/2026-07-09-v2-rebuild-design.md`（调研引用见 docs/superpowers/research/）。

**Tech Stack:** better-sqlite3（WAL+BEGIN IMMEDIATE）、TypeScript ESM、vitest。

**执行约定：** 工作目录 `~/projects/subtitle-scout`，分支 `v2-core`（从 main 切）。sonnet-5 子代理，
每 task 结束 `npx tsc --noEmit && npx vitest run` 全绿才提交。**一期不部署生产**（生产继续跑 v1
直到二期 dashboard 完成后走验收仪式）。旧 dashboard 在一期期间数据源断供属预期，不修。

---

### Task 1: 依赖与 DB 基座

**Files:**
- Create: `src/v2/db.ts`
- Test: `src/v2/db.test.ts`
- Modify: `package.json`（dependencies + better-sqlite3；devDependencies + @types/better-sqlite3）

- [ ] **Step 1:** `npm i better-sqlite3 --no-audit && npm i -D @types/better-sqlite3 --no-audit`
- [ ] **Step 2: 失败测试** `src/v2/db.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { mkdtempSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { openDb } from './db.js'

describe('db 基座', () => {
  it('打开即建 schema，pragma 三件套生效', () => {
    const db = openDb(join(mkdtempSync(join(tmpdir(), 'scout-')), 'scout.db'))
    expect(db.pragma('journal_mode', { simple: true })).toBe('wal')
    expect(db.pragma('busy_timeout', { simple: true })).toBe(5000)
    const tables = db.prepare("select name from sqlite_master where type='table' order by name").all().map((r: any) => r.name)
    for (const t of ['series', 'episodes', 'movies', 'jobs', 'runs', 'subtitles', 'blacklist', 'meta']) expect(tables).toContain(t)
    expect(db.prepare("select value from meta where key='schema_version'").get()).toEqual({ value: '1' })
  })
  it('重复打开幂等（不重跑建表）', () => {
    const p = join(mkdtempSync(join(tmpdir(), 'scout-')), 'scout.db')
    openDb(p).close(); const db2 = openDb(p)
    expect(db2.prepare("select value from meta where key='schema_version'").get()).toEqual({ value: '1' })
  })
})
```

- [ ] **Step 3:** 跑 `npx vitest run src/v2/db.test.ts` → FAIL（模块不存在）
- [ ] **Step 4: 实现** `src/v2/db.ts`：`openDb(path): Database` —— `new Database(path)`；
  pragma `journal_mode=WAL`、`busy_timeout=5000`、`synchronous=NORMAL`、`foreign_keys=ON`；
  迁移器：读 meta.schema_version（表不存在=0），在 `db.transaction` 里按序执行 MIGRATIONS 数组，
  v1 迁移=spec §1 的完整 DDL 原文（series/episodes/movies/jobs/runs/subtitles/blacklist/meta +
  jobs_identity 唯一索引），末尾写 schema_version。导出 `export type ScoutDb = Database.Database`。
  DDL 逐字从 spec §1 抄，不许改字段名。
- [ ] **Step 5:** 测试过 → `git add -A && git commit -m "feat(v2): sqlite foundation — pragmas, schema v1, idempotent migrations"`

### Task 2: jobs 状态机仓库（领取/租约/退避）

**Files:**
- Create: `src/v2/jobsRepo.ts`
- Test: `src/v2/jobsRepo.test.ts`

- [ ] **Step 1: 失败测试**（核心不变量全覆盖）：

```ts
import { describe, it, expect, beforeEach } from 'vitest'
// openDb 到内存库 ':memory:'；每用例新库
import { openDb } from './db.js'
import { JobsRepo, CONTENT_BACKOFF_DAYS, errorBackoffMs } from './jobsRepo.js'

let repo: JobsRepo
beforeEach(() => { repo = new JobsRepo(openDb(':memory:')) })
const mkSeriesJob = () => repo.upsertWanted({ kind: 'series_season', seriesId: 's1', season: 4 })

describe('jobs 状态机', () => {
  it('upsertWanted 幂等：同剧同季只有一行', () => {
    mkSeriesJob(); mkSeriesJob()
    expect(repo.countByState('wanted')).toBe(1)
  })
  it('claimNext 原子领取：置 searching+租约，二次领取拿不到同一 job', () => {
    mkSeriesJob()
    const a = repo.claimNext(Date.now(), 'w1')
    expect(a?.state).toBe('searching')
    expect(repo.claimNext(Date.now(), 'w2')).toBeNull()
  })
  it('优先级高者先领', () => {
    mkSeriesJob()
    repo.upsertWanted({ kind: 'movie', movieId: 'm1' }); repo.boostPriority({ kind: 'movie', movieId: 'm1' }, 100)
    expect(repo.claimNext(Date.now(), 'w')?.movie_id).toBe('m1')
  })
  it('过租 job 被 reap 归位 wanted 且 attempt+1', () => {
    mkSeriesJob()
    const j = repo.claimNext(Date.now(), 'w')!
    repo.reapExpiredLeases(Date.now() + 31 * 60_000)
    const again = repo.claimNext(Date.now() + 31 * 60_000, 'w')
    expect(again?.id).toBe(j.id); expect(again?.attempt).toBe(1)
  })
  it('内容性失败指数退避：1/2/4/8 天后 dormant', () => {
    mkSeriesJob()
    for (const days of CONTENT_BACKOFF_DAYS) {
      const j = repo.claimNext(Date.now(), 'w') ?? repo.forceClaim('s1', 4)  // 测试助手：无视 next_retry_at 领取
      repo.completeNoMatch(j!.id, Date.now())
      const row = repo.get(j!.id)!
      if (row.state === 'dormant') break
      expect(row.state).toBe('failed')
      expect(row.next_retry_at! - Date.now()).toBeGreaterThanOrEqual(days * 86_400_000 - 1000)
    }
    expect(repo.get(repo.find('s1', 4)!.id)!.state).toBe('dormant')
  })
  it('错误性失败短退避且与内容性分流', () => {
    mkSeriesJob()
    const j = repo.claimNext(Date.now(), 'w')!
    repo.completeError(j.id, 'ASSRT 500', Date.now())
    const row = repo.get(j.id)!
    expect(row.state).toBe('failed')
    expect(row.next_retry_at! - Date.now()).toBeLessThanOrEqual(errorBackoffMs(1) + 1000)
  })
  it('部分成功 attempt 减 1 而非清零（escalation 渐进恢复）', () => {
    mkSeriesJob()
    let j = repo.claimNext(Date.now(), 'w')!
    repo.completeNoMatch(j.id, Date.now())            // attempt 1
    j = repo.forceClaim('s1', 4)!
    repo.completePartial(j.id, Date.now())            // attempt 应回 0
    expect(repo.get(j.id)!.attempt).toBe(0)
    expect(repo.get(j.id)!.state).toBe('wanted')      // 部分成功→回 wanted 继续追残集
  })
  it('completeDone 置 done 终态', () => {
    mkSeriesJob()
    const j = repo.claimNext(Date.now(), 'w')!
    repo.completeDone(j.id, Date.now())
    expect(repo.get(j.id)!.state).toBe('done')
  })
  it('唤醒 dormant（播放触发语义）', () => {
    mkSeriesJob()
    repo.forceState('s1', 4, 'dormant')               // 测试助手
    repo.wake({ kind: 'series_season', seriesId: 's1', season: 4 }, 100)
    const row = repo.find('s1', 4)!
    expect(row.state).toBe('wanted'); expect(row.priority).toBe(100); expect(row.next_retry_at).toBeNull()
  })
})
```

- [ ] **Step 2:** FAIL 确认
- [ ] **Step 3: 实现** `src/v2/jobsRepo.ts`。要点：
  - `claimNext(now, workerId)`：**单条** `UPDATE jobs SET state='searching', lease_until=?, updated_at=?
    WHERE id=(SELECT id FROM jobs WHERE state='wanted' AND (next_retry_at IS NULL OR next_retry_at<=?)
    ORDER BY priority DESC, created_at ASC LIMIT 1) RETURNING *`（better-sqlite3 `.get()`）；租约 30 分钟常量。
  - `reapExpiredLeases(now)`：`UPDATE jobs SET state='wanted', attempt=attempt+1, lease_until=NULL
    WHERE state IN ('searching','downloading','verifying') AND lease_until < ?`。
  - `export const CONTENT_BACKOFF_DAYS = [1,2,4,8]`；`completeNoMatch`：attempt+1，attempt>4→dormant，
    否则 state='failed' + next_retry_at=now+CONTENT_BACKOFF_DAYS[attempt-1]天。
  - `export const errorBackoffMs = (attempt: number) => Math.min(attempt * 10 * 60_000, 6 * 3600_000)`；
    `completeError` 同构短退避。
  - `completePartial`：attempt=max(0,attempt-1)，state='wanted'，next_retry_at=null。
  - `completeDone` / `boostPriority` / `wake` / `find(seriesId, season)` / `get(id)` / `countByState` /
    测试助手 `forceClaim`、`forceState`（仅供测试，导出并注释）。
  - 所有多语句操作包 `db.transaction`。failed 状态在 claimNext 的 WHERE 里等同 wanted 处理
    ——修正：`WHERE state IN ('wanted','failed')`，dormant 不参与。**测试里对应断言以此为准。**
- [ ] **Step 4:** 测试过 + 全量绿 → commit `feat(v2): jobs state machine — atomic claim, lease reaping, dual-track backoff`

### Task 3: 媒体镜像仓库（library repo）

**Files:**
- Create: `src/v2/libraryRepo.ts`
- Test: `src/v2/libraryRepo.test.ts`

- [ ] **Step 1: 失败测试**：

```ts
import { openDb } from './db.js'
import { LibraryRepo } from './libraryRepo.js'
// beforeEach 新内存库
describe('媒体镜像', () => {
  it('upsertEpisode 幂等且更新时间戳', () => {
    const ep = { id: 'e1', seriesId: 's1', season: 1, episode: 2, name: 'x', path: '/tv/a/e2.mkv', subStatus: 'missing' as const }
    lib.upsertSeries({ id: 's1', name: 'A' }); lib.upsertEpisode(ep); lib.upsertEpisode({ ...ep, subStatus: 'covered' })
    expect(lib.getEpisode('e1')!.sub_status).toBe('covered')
    expect(lib.db.prepare('select count(*) c from episodes').get().c).toBe(1)
  })
  it('missingBySeason 聚合缺字幕的 (series,season) 组', () => {
    lib.upsertSeries({ id: 's1', name: 'A' })
    for (const [e, st] of [[1, 'missing'], [2, 'missing'], [3, 'covered']] as const)
      lib.upsertEpisode({ id: `e${e}`, seriesId: 's1', season: 1, episode: e, name: '', path: `/p/${e}`, subStatus: st })
    expect(lib.missingBySeason()).toEqual([{ series_id: 's1', season: 1, missing: 2 }])
  })
  it('markCovered 写 episodes.sub_status + subtitles 行，同一事务', () => {
    lib.upsertSeries({ id: 's1', name: 'A' })
    lib.upsertEpisode({ id: 'e1', seriesId: 's1', season: 1, episode: 1, name: '', path: '/p/1.mkv', subStatus: 'missing' })
    lib.markCovered('e1', '/p/1.zh-Hans.srt', 'scout-download', 713051)
    expect(lib.getEpisode('e1')!.sub_status).toBe('covered')
    expect(lib.db.prepare('select * from subtitles where item_id=?').get('e1')).toMatchObject({ path: '/p/1.zh-Hans.srt', assrt_sub_id: 713051 })
  })
  it('unavailable 带复查时间，missingBySeason 不计入未到期的', () => {
    lib.upsertSeries({ id: 's1', name: 'A' })
    lib.upsertEpisode({ id: 'e1', seriesId: 's1', season: 1, episode: 1, name: '', path: '/p/1', subStatus: 'missing' })
    lib.markUnavailable('e1', '搜索穷尽', Date.now() + 86_400_000)
    expect(lib.missingBySeason()).toEqual([])
  })
})
```

- [ ] **Step 2:** FAIL → **Step 3: 实现**（upsert 用 `INSERT ... ON CONFLICT(id) DO UPDATE`；
  movies 同构方法 `upsertMovie/missingMovies/markMovieCovered`；导出 `lib.db` 供测试）
- [ ] **Step 4:** 绿 → commit `feat(v2): library mirror repo — idempotent upserts, coverage transactions`

### Task 4: 扫描器（Jellyfin → 镜像）

**Files:**
- Create: `src/v2/scanner.ts`
- Test: `src/v2/scanner.test.ts`
- Modify: `src/adapters/players/jellyfin.ts` + `src/adapters/players/types.ts`（加分页方法）

- [ ] **Step 1:** PlayerServer 加 `getItemsPage(startIndex: number, limit: number): Promise<MediaItem[]>`；
  JellyfinClient 实现：`/Items?recursive=true&includeItemTypes=Movie,Episode&sortBy=DateCreated&sortOrder=Ascending&startIndex=${s}&limit=${l}&fields=${ITEM_FIELDS},DateCreated`。
  jellyfin.test.ts 加一个 fixture 用例断言 URL 含 startIndex。
- [ ] **Step 2: scanner 失败测试**：

```ts
import { scanLibrary, classifyItem } from './scanner.js'
describe('scanner', () => {
  it('classifyItem：内嵌中字→embedded；外挂在盘→covered；国产→ignored；其余→missing', () => {
    // 用 fixtures/jellyfin/items-detail.json 的真实 item 形状构造四种输入，
    // needsChineseSubtitle / SKIP_CHINESE_ORIGIN 判定逻辑复用 src/daemon/triggers.ts 的现有导出
    // deps 注入 fileExists: (p) => boolean 判外挂
  })
  it('scanLibrary 分页拉全库、镜像入库、二跑幂等', async () => {
    const pages = [[epItem('e1'), epItem('e2')], [movieItem('m1')], []]
    const jf = { getItemsPage: vi.fn(async (s: number) => pages[Math.floor(s / 2)] ?? []) }
    await scanLibrary(jf as never, lib, { pageSize: 2, fileExists: () => false, skipChineseOrigin: true })
    await scanLibrary(jf as never, lib, { pageSize: 2, fileExists: () => false, skipChineseOrigin: true })
    expect(lib.db.prepare('select count(*) c from episodes').get().c).toBe(2)
    expect(lib.db.prepare('select count(*) c from movies').get().c).toBe(1)
  })
})
```

- [ ] **Step 3:** FAIL → 实现：`classifyItem(item, deps): SubStatus`（判定次序：国产 ignored →
  MediaStreams 有中字轨（IsExternal=false）→ embedded → 盘上有外挂（deps.fileExists 对
  mapPath 后的 sidecar 探测，探测逻辑抄 daemon/triggers.ts 的现有实现）→ covered → missing）；
  `scanLibrary(jf, lib, opts)`：while 分页到空页；Episode→upsertSeries(去重)+upsertEpisode；
  Movie→upsertMovie；**只在状态不同才写**（幂等+少写放大）；结束写 meta.last_scan_at。
- [ ] **Step 4:** 绿 → commit `feat(v2): library scanner — paged full scan, status classification, idempotent mirror`

### Task 5: 聚合器（missing → jobs）

**Files:**
- Create: `src/v2/aggregator.ts`
- Test: `src/v2/aggregator.test.ts`

- [ ] **Step 1: 失败测试**：`missingBySeason` 两组 → `aggregate(lib, jobs)` 后 jobs 表两行 wanted；
  再跑一遍行数不变（幂等）；某季全部 covered 后再 aggregate，该 job 若仍 wanted 且 target 空→置 done。
- [ ] **Step 2:** FAIL → 实现：对 `lib.missingBySeason()` 每组 `jobs.upsertWanted({kind:'series_season',...})`；
  对 `lib.missingMovies()` 每部 `upsertWanted({kind:'movie',...})`；清理：wanted 状态且重derive为空的 job → done。
- [ ] **Step 3:** 绿 → commit `feat(v2): aggregator — missing coverage to season-level jobs`

### Task 6: 剧级执行器（复用 v1 判断链）

**Files:**
- Create: `src/v2/executor.ts`
- Test: `src/v2/executor.test.ts`

先读（integration 契约，实现前必读）：`src/core/pipeline.ts:23-56`（PipelineDeps——assrt/download/cache/
seasonPack.onCovered 挂点）、`src/cli/index.ts:84-110`（assemble().makeDeps 如何构造 deps）、
`src/cli/index.ts:184-215`（cmdRunItem 如何组 ctx + verify）。

- [ ] **Step 1: 失败测试**（pipeline 以注入函数 stub，不碰真实现）：

```ts
interface ExecutorDeps {
  lib: LibraryRepo; jobs: JobsRepo
  runEpisode: (episodeId: string, onCovered: (epId: string, subPath: string) => void) => Promise<{ decision: string }>
  now: () => number
}
describe('executor', () => {
  it('重derive targets：跑前用户手动放了字幕的集不再处理', async () => { /* e1 covered 后 executeJob 只跑 e2 */ })
  it('季包覆盖：runEpisode stub 触发 onCovered(e1..e3) → 三集 covered + job done + runs 记录', async () => {})
  it('部分覆盖：只 covered e1 → completePartial（job 回 wanted, attempt-1），已覆盖战果保留', async () => {})
  it('全军覆没 no_safe_match → completeNoMatch + 未覆盖集不改状态', async () => {})
  it('runEpisode 抛错 → completeError，短退避', async () => {})
  it('movie job 同构', async () => {})
})
```

- [ ] **Step 2:** FAIL → 实现 `executeJob(job, deps)`：
  1. 重derive：series job → `lib` 查该 (series,season) 当前 missing 集；空 → completeDone 返回。
  2. 选代表集（最小集号）调 `deps.runEpisode(epId, onCovered)`——onCovered 里 `lib.markCovered` +
     若是本 job target 内的集顺带记数（v1 季包路径会对每个命中集回调，天然批量）。
  3. 按结果分流：全覆盖→completeDone；部分→completePartial；no_safe_match→completeNoMatch
     （若 targets 全部 unavailable 化则同步 `lib.markUnavailable` 各集，recheck=next_retry_at）；
     异常→completeError。每次执行写一行 runs（decision/detail 人话摘要/journal_path/耗时）。
- [ ] **Step 3: 真实 runEpisode 接线**（新函数 `makeRunEpisode(assembled)` 放 executor.ts 底部）：
  组 ctx（jf.getItem→buildMediaContext+getChineseTitle，抄 cmdRunItem 的组法）→
  `withJournal(() => runPipeline(makeDeps({ itemId, onCovered }), ctx, mediaDir))` → 映射 decision。
  tsc 通过即可（真机验证在验收仪式，一期不跑生产）。
- [ ] **Step 4:** 绿 → commit `feat(v2): season-level executor — re-derive targets, partial credit, pipeline reuse`

### Task 7: 客户端超时全覆盖（审计 C 轨）

**Files:**
- Modify: `src/adapters/players/jellyfin.ts`（call() 加 `signal: AbortSignal.timeout(30_000)`）
- Modify: `src/adapters/providers/assrt.ts`（fetch 加 15s——放在重试循环**内**，超时可触发重试）
- Modify: `src/adapters/download/direct.ts`（60s，同理重试内）
- Modify: `src/agent/llm.ts`（两处 generateText 加 `abortSignal: AbortSignal.timeout(120_000)`）
- Test: 各自 test 文件加一个用例（fetchImpl 挂起 never-resolve + vi.useFakeTimers 或直接断言
  fetch 收到 signal 参数——**用后者**：`expect(fetchImpl.mock.calls[0][1].signal).toBeInstanceOf(AbortSignal)`，简单可靠）

- [ ] Step 1 各文件失败测试（断言 signal 传入）→ Step 2 实现（默认值常量导出，方便 doctor 复用）→
  Step 3 全量绿 → commit `fix(clients): AbortSignal timeouts everywhere — no more global hangs (audit C)`

### Task 8: v2 daemon 主循环

**Files:**
- Create: `src/v2/daemon.ts`
- Test: `src/v2/daemon.test.ts`
- Modify: `src/cli/index.ts`（`watch` 改走 v2 daemon；旧 `src/daemon/watcher.ts` 保留文件但不再被 watch 引用）

- [ ] **Step 1: 失败测试**（注入假 clock/组件，断言编排逻辑）：
  - tick 序：reapExpiredLeases → (到点) scanLibrary+aggregate → dispatcher 领取至并发上限
  - sessions 轮询命中缺字幕条目 → jobs.wake/boostPriority(100)
  - dispatcher：searching 并发 1、downloading/verifying 各 2（以 jobs 表状态计数为准）
  - 单个 executeJob 抛错不炸主循环（记日志继续）
  - SIGTERM：不再领新 job，等在跑的完成或过租，退出
- [ ] **Step 2:** FAIL → 实现：`class ScoutDaemon { constructor(deps){} async tick(){} run(signal){} }`；
  reconcile 间隔 15min、全量对账 6h（meta.last_full_scan_at）、sessions 轮询 15s 独立 setInterval
  （只调 getSessions 一个接口，有超时保护，挂了跳过本拍——**与 job 执行解耦，互不阻塞**）。
- [ ] **Step 3:** cli 接线：`cmdWatch` 构造 assembled + repos + daemon.run()；dashboard server 暂不起
  （二期接 DB 后恢复）。
- [ ] **Step 4:** 绿 → commit `feat(v2): daemon — reconcile loop, staged dispatcher, decoupled session poll`

### Task 9: doctor 扩展（第 6/7 项）

**Files:**
- Modify: `src/cli/doctor.ts` + `src/cli/doctor.test.ts` + `src/cli/index.ts`（cmdDoctor 追加两项）

- [ ] TDD：`checkDatabase(open: () => {version: string})`（可开+版本匹配→✓）；
  `checkStuckJobs(count: () => number)`（过租 job 数 0→✓，>0→✗ 带人话提示"有 N 个任务卡住，
  重启会自动归位，若反复出现请提 issue"）→ 接线 → commit `feat(doctor): db + stuck-jobs checks`

### Task 10: 崩溃恢复与幂等测试套件

**Files:**
- Create: `src/v2/recovery.test.ts`

- [ ] 用例（全部内存库+假 clock，不起真 daemon）：
  1. **kill-mid-flight**：claim 后直接新开 repo 实例（模拟重启）→ reap → job 回 wanted，attempt+1
  2. **双跑幂等**：同一 jf stub 现实，scan+aggregate 连跑两遍 → episodes/jobs 行集完全一致（快照对比）
  3. **markCovered 与 job 完成同事务**：injected 事务中途抛错 → episodes 与 runs 都未写入（原子性）
  4. **dormant 不复活**：reap/aggregate 均不得把 dormant 拉回 wanted（只有 wake 可以）
- [ ] 绿 → commit `test(v2): crash recovery + reconcile idempotency suite`

### Task 11: 一期收口

- [ ] `npx tsc --noEmit && npx vitest run` 全绿（预期 300+）
- [ ] opus 终审整个 v2-core 分支（对照 spec §1-§3 + 审计 B/C 轨逐条销项：队头饿死/error 退避/
  watermark/超时/inFlight 泄漏——每条指出 v2 中对应的消灭机制与测试）
- [ ] 合 main（--no-ff），**不部署生产**
- [ ] 更新记忆：一期完成状态 + 二期入口（dashboard：先通读 design-taste-frontend，设计稿过 visual companion）
- [ ] I5c verify 可见性复验（二期 verifying 阶段）+ I5e bypass 语义复核

## Self-Review 记录

- spec 覆盖：§1→T1-3、§2→T4-6+T8、§3→T2/T7/T8、doctor→T9、崩溃/幂等→T10 ✓；§4 dashboard=二期，§5 验收仪式=二期后 ✓
- 类型一致：JobsRepo 方法名在 T2 定义、T5/T6/T8/T10 引用处一致（upsertWanted/claimNext/completeDone/
  completePartial/completeNoMatch/completeError/reapExpiredLeases/wake/boostPriority）✓
- 无 TBD/占位；T6 的 pipeline 接线给了必读文件行号与真实挂点（PipelineDeps.seasonPack.onCovered）✓
- 审计销项映射：B1 队头饿死→jobs 无"跳过不出队"路径（一切结果都有 complete* 迁移）+T10.1；
  B2 error 无退避→completeError 短退避+分流；B3 watermark→T4 全量分页+6h 对账；
  C 超时→T7+租约兜底；inFlight 泄漏→租约替代内存 inFlight ✓
