# 收尾计划 · TDD + subagent driven

**建立**: 2026-08-13
**上游**: `2026-08-11-FRONTEND-IMPL-PLAN.md`（12 task 已全部落地）
**执行模式**: 每个 task 由 subagent **TDD 实现**（先写红测试 → 实现 → 变异验证）
→ **另起 subagent 对抗审计** → 我裁决 → commit

---

## 零、给每个 subagent 的通用底座（**必须贴进每次 prompt**）

### 验收：只用一条命令

```bash
npm run verify
```

四条判据缺一即失败：① 退出码 0 ② `numFailedTests === 0`
③ **每个 `testResults[].status === 'passed'`** ④ 文件数 >= 基线。

⚠️ **不许再手敲 `numFailedTests` 那套口径**——它对**空 describe** 完全免疫。
2026-08-13 栽过一次：commit message 写「146 文件 / 3272 用例 / 0 失败」字面为真，
而 vitest **退出码是 1**（两个 `describe` 被删空只剩壳）。`scripts/verify.mjs` 就是为此建的。

**当前基线**：后端 146 文件 / 3284 用例；前端 84 文件 / 975 用例。

### 本仓两个招牌病

| 病 | 形态 | 已栽 |
|---|---|---|
| **A** | 加了能力却没定**谁写 / 谁读 / 谁触发** | 12 次 |
| **B** | 把**中间量**说成**结论量** | 6 次 |

### ⚠️ 变异验证的十个陷阱（都真踩过）

1. 模块级 `throw` → vitest 报 `total=0`（**整个文件没跑**，不是"没变红"）
2. 探针插进块注释里 → 0 命中
3. `?.` 注入成了 no-op
4. 变异有类型错误 → `tsc≠0`，那时的红**全是连带**
5. 变异被自己的**注释字符串**匹配到 → 假阳性
6. `noUnusedLocals` 让删除型变异报 TS6133 → 同 4
7. anchor 不匹配 → 变异**根本没注入**
8. 空 `describe` → `numFailedTests` 看不见（见上）
9. **守卫自己是假的**：`as` 断言 / `Object.fromEntries` 会压掉类型检查；
   `_Missing[] = []` 对任何数组类型都成立
10. **"生产 0 行"不能证明死活**——本仓活表 `item_files` 同样 0 行，只有**静态可达性**能证明

**铁律：变异后先 `grep` 确认它落在可执行代码行 + 确认 `tsc=0`，才采信红数。**

### TDD 纪律（本轮新增）

先写**会红**的测试，再实现。提交前必须能回答：
「我这条测试，在实现回退到旧行为时会不会红？」——**用变异证明，不用嘴说。**

---

## 一、Task 清单

### T1 · 孤儿族整族裁决（1803 行）🔴 最大一块

`handleWorkerTask` / `unidentifiedFindSubtitle` / `realignWorkerTask` / `workUnit` /
`subtitlePropagation` / `verifySweep` —— 它们**互相引用构成封闭族**（只有族内边，无外部入口）。

三个 `.orphan.test.ts` 守卫今天仍绿 = 判据仍成立。

**要判的**：
- `subtitleVerify` 族的判据 (b) 是「距 2026-08-12 再过一个发布周期」——**到期了吗**？
- 整族删掉之后，`parked_paths` / `item_files` / `jobs` 三张表怎么办？
- ⚠️ `addReplicaSubtitle` 是 verify 族 246 条算法用例的 fixture——删了那批用例还能跑吗？

**这是产品决策，subagent 只出方案与证据，我拍板。**

### T2 · 口径统一（3 处已知重复定义）

- `skip_reason='extra'` 判据在 `classifyFileState` 与 `fileIsExtra` **各写一遍**
- 前端 `LEGEND_STATES` 与后端 `STATE_RANK` 同序**只靠注释约定**，无测试钉住
- 多处注释把「生产 0 行」当死代码证据（论据不成立，见陷阱 10）

### T3 · `aggregateDot` 的 fileCount 把特典算进去

用户会看到电影格 `fileCount=2` 而其中一份是 Trailer。呈现口径问题。

### T4 · 翻译台的空态歧义（与已修的 🔴-1 同型）

`listNewTranslateCandidates` 的 `tr_recheck_after` 没有短路形参 →
翻译队列全在退避窗时仍说「没有排队的作品」。字幕台已修，翻译台没修。

### T5 · 季集解析器（112 个 NULL 里 63 个是真剧集）

那 63 个是解析器在**正常剧集**上失败（`[01]` 括号数字、日文 `第05話`）。
本轮做的是「给进不了网格的文件一个显式表达」，不是「让它们进网格」。

⚠️ 这条**优先级最高**：它是用户能直接感知的（媒体库少显示 63 集）。

### T6 · 收尾

- 文档同步（`CURRENT-STATE.md` 的基线与进度）
- 生产部署 + 验证
- 债务清单归档

---

## 二、执行顺序

```
T5（用户可感知）→ T4（同型缺陷）→ T3 → T2（口径）→ T1（产品决策，最后）→ T6
```

T1 放最后：它是删代码，而删代码前应该先把**活功能的问题**修完。

---

## 三、每个 task 完成后的对抗审计（固定动作）

```
你是对抗性审计员。目标：证明这次实施是错的 / 不完整的 / 是假修复。

【不许推测，必须实跑】每条结论附命令与真实输出。
【验收只用 npm run verify】四条判据，不许手敲旧口径。

审计四层（每层都抓到过 🔴）：
1. 事实层：报告里每个数字实跑核对
2. 遗漏层：新加的字段/端点/函数，逐个说出【谁写/谁读/谁触发】
3. 决策层：与 R-F1~R-F15 冲突吗？与生产现有代码冲突吗？
4. 假修复：改断言 / ?. / 吞异常 / as any？把中间量说成结论量？
   ⚠️ 特别查【守卫自己是不是假的】——用 as / fromEntries 压掉类型检查的，本轮抓到过

最后必须回答：
  A. 问题分级（🔴必修/🟡该修/🔵可不修），每条附实跑证据
  B. 【你攻击过但没攻破的】——区分"真没问题"与"没认真找"
  C. 用户视角一句话：界面上还有没有说假话的地方？
```

---

## 四、进度

| Task | 状态 | commit |
|---|---|---|
| T5 季集解析器 | ⬜ | |
| T4 翻译台空态 | ⬜ | |
| T3 fileCount | ⬜ | |
| T2 口径统一 | ⬜ | |
| T1 孤儿族裁决 | ⬜ | |
| T6 收尾 | ⬜ | |
