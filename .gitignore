.serena/
node_modules/
# 不带斜杠的那条也要：符号链接不是目录，`node_modules/` 匹配不到它。
# 2026-07-30 真实事故——worktree 里 `ln -s` 指向主仓的 node_modules，那个链接被当成普通
# 文件提交了（314f95a），随后 merge 到 main 时把主仓真实的 node_modules 目录替换成一个
# 指向自己的循环链接，vitest 直接 exit 194 零输出。
node_modules
.wrangler/
.dev.vars
scratch/
scratchpad/
.env
.env.*
!.env.example
.superpowers/
dist/

# 作者私有（不随公开仓；本地保留）
deploy/
!deploy/
deploy/*
!deploy/deploy.sh
# router.env.example 是纯模板（不含真值），应入库：否则对它的修复只存在于本机
!deploy/router.env.example
worker/
# 内部设计/战役/事故记录整目录不随公开仓（开源时不带内部过程文档）
docs/
# 例外：docs/design/ 是重构的唯一依据（spec + 审计报告），丢了无法复原，必须入库。
# 开源时如需剥离，删这两行即可（用户裁决 2026-08-08）
!docs/
!docs/design/
jellyfin-config/
cache/

# 本地 e2e 测试栈（scripts/gen-mock-library.sh + docker-compose.local.yml）
fixtures/media/
cache-local/

# v3 live matrix runner scratch (OrbStack recipe cache mount)
matrix-scratch/
.research/
.claude/
.omo/
.opencode/
