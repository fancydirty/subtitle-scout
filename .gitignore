.serena/
node_modules/
.wrangler/
.dev.vars
scratch/
scratchpad/
.env
.env.*
!.env.example
.superpowers/
dist/

# 作者私有（不随公开仓；本地保留）
deploy/
!deploy/
deploy/*
!deploy/deploy.sh
# router.env.example 是纯模板（不含真值），应入库：否则对它的修复只存在于本机
!deploy/router.env.example
worker/
# 内部设计/战役/事故记录整目录不随公开仓（开源时不带内部过程文档）
docs/
jellyfin-config/
cache/

# 本地 e2e 测试栈（scripts/gen-mock-library.sh + docker-compose.local.yml）
fixtures/media/
cache-local/

# v3 live matrix runner scratch (OrbStack recipe cache mount)
matrix-scratch/
.research/
.claude/
.omo/
.opencode/
