# 全家桶：从零开始，Jellyfin + subtitle-scout 一起拉起。
# 已有 Jellyfin 的用户请用 docker-compose.yml（独立版）。
# 用法：cp .env.example .env 填好三把钥匙与 MEDIA_HOST_PATH，然后
#   docker compose -f docker-compose.bundle.yml up -d
# 注意：Jellyfin 首次运行需先完成初始向导（http://<主机>:8096）并生成 API key，
# 填入 .env 的 JELLYFIN_API_KEY 后重启 scout：docker compose -f docker-compose.bundle.yml restart subtitle-scout
services:
  jellyfin:
    image: ghcr.io/jellyfin/jellyfin:latest
    container_name: scout-jellyfin
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "8096:8096"
    volumes:
      - ./jellyfin-config:/config
      - ${MEDIA_HOST_PATH}/Movies:/media/movies
      - ${MEDIA_HOST_PATH}/TV:/media/tv

  subtitle-scout:
    image: ghcr.io/fancydirty/subtitle-scout:latest
    container_name: subtitle-scout
    restart: unless-stopped
    # 容器内 PID 1 是 node（Dockerfile CMD 直接跑 dist/cli/index.js）——但普通进程
    # 不具备 init 的孤儿收割职责。providerPort.ts 里每次字幕抓取都 detached 起一个独立
    # 进程组的 subtitle-fetch 子进程；主进程若异常退出，该子进程会被重新收养给 PID 1，
    # 而 node 不会像 tini 那样自动 wait() 收割不认识的子孙进程，久而久之堆成僵尸。
    # init:true 让 Docker 注入内置 tini 作真正的 PID 1，负责收割 + 正确转发终止信号。
    init: true
    depends_on:
      - jellyfin
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "${DASHBOARD_PORT:-8099}:${DASHBOARD_PORT:-8099}"
    environment:
      JELLYFIN_URL: http://jellyfin:8096
      JELLYFIN_API_KEY: ${JELLYFIN_API_KEY}
      LLM_BASE_URL: ${LLM_BASE_URL}
      LLM_API_KEY: ${LLM_API_KEY}
      LLM_MODEL: ${LLM_MODEL}
      LLM_EXTRA_BODY: ${LLM_EXTRA_BODY:-}
      ASSRT_TOKEN: ${ASSRT_TOKEN}
      TMDB_API_KEY: ${TMDB_API_KEY:-}
      OPENSUBTITLES_API_KEY: ${OPENSUBTITLES_API_KEY:-}
      OPENSUBTITLES_USERNAME: ${OPENSUBTITLES_USERNAME:-}
      OPENSUBTITLES_PASSWORD: ${OPENSUBTITLES_PASSWORD:-}
      MEDIA_ROOTS: /media
      SUBTITLE_SCOUT_CACHE_DIR: /cache
      POLL_INTERVAL_SECONDS: ${POLL_INTERVAL_SECONDS:-15}
      TZ: ${TZ:-Asia/Shanghai}
      DASHBOARD_PORT: ${DASHBOARD_PORT:-8099}
      DASHBOARD_TOKEN: ${DASHBOARD_TOKEN:-}
    volumes:
      # 与 jellyfin 服务完全相同的挂载 → 路径天然一致，无需 MEDIA_PATH_MAPPINGS
      - ${MEDIA_HOST_PATH}/Movies:/media/movies
      - ${MEDIA_HOST_PATH}/TV:/media/tv
      - ./cache:/cache
